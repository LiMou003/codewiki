哈 尔 滨 工 业 大 学

本科毕业论文（设计）书写范例

（理工类）

本科毕业论文（设计）

基于RAG检索的智能代码问答系统设计与实现

**DESIGN AND IMPLEMENTATION OF AN INTELLIGENT CODE QUESTION-ANSWERING SYSTEM BASED ON RAG RETRIEVAL**

**李栋梁**

**哈尔滨工业大学**

**2026年6月**

密级：公开

**本科毕业论文（设计）**

基于RAG检索的智能代码问答系统设计与实现

|     |     |     |
| --- | --- | --- |
| 本科生 | ：   | 李栋梁 |
| 学号  | ：   | 2022212021 |
| 指导教师 | ：   | 王金宝教授 |
| 专业  | ：   | 软件工程 |
| 学院  | ：   | 软件学院 |
| 答 辩 日 期 | ：   | 2026年6月 |
| 学校  | ：   | 哈尔滨工业大学 |

# 摘 要

随着软件系统规模与复杂度持续提高，文档滞后与知识碎片化问题已成为软件开发中的普遍痛点。一方面在软件开发过程中跨语言、跨模块、跨仓库协作变为常态，代码面临着更迭频繁从而导致文档与代码不同步；另一方面，开发者在进行跨文件的问题定位、依赖关系梳理与调用链梳理过程中成本高、效率低，严重影响知识传承与协同开发。

近年来，检索增强生成（Retrieval-Augmented Generation,RAG）在知识密集型任务中被证明可以显著降低幻觉并提升一致性，为"先定位证据、再生成答案"的范式提供了基础<sup>\[1\]</sup>。

基于上述提到的情况，本文构建了一个基于检索增强生成（RAG）的代码知识智能问答系统，针对大型软件项目中文档滞后、知识碎片化、跨文件理解成本高等问题，实现代码仓库知识的自动提取、持续更新与高质量问答，从而提升开发者的代码理解效率、降低知识传承成本、增强跨团队协作能力。

本文给出了系统的需求分析、概要设计、详细设计，需求分析包括功能性需求分析和非功能性需求分析，概要设计包括系统架构设计、功能模块设计、数据库设计和接口定义设计，详细设计包括对系统各个功能模块具体流程实现设计。此外还给出了系统的实现描述和实现展示，针对系统实现功能设计测试用例并给出测试结果。

**关键词**:团队协作开发；检索增强生成；大模型生成

# Abstract

As the scale and complexity of software systems continue to increase, issues such as documentation lag and knowledge fragmentation have become common pain points in software development. On the one hand, cross-language, cross-module, and cross-repository collaboration has become the norm in the software development process, leading to frequent code changes that result in a lack of synchronization between documentation and code. On the other hand, the process of identifying issues across files, sorting out dependency relationships, and tracing call chains is costly and inefficient for developers, seriously affecting knowledge inheritance and collaborative development .

In recent years, Retrieval-Augmented Generation (RAG) has been proven to significantly reduce hallucinations and enhance consistency in knowledge-intensive tasks, laying the foundation for the paradigm of "locating evidence first, then generating answers".

Based on the aforementioned situation, this paper constructs an intelligent question-answering system for code knowledge based on Retrieval Augmented Generation (RAG). This system addresses issues such as document lag, knowledge fragmentation, and high cross-file comprehension costs in large software projects. It achieves automatic extraction, continuous updating, and high-quality question-answering of code repository knowledge, thereby enhancing developers' code comprehension efficiency, reducing knowledge inheritance costs, and enhancing cross-team collaboration capabilities.

This article presents the system's requirements analysis, high-level design, and detailed design. The requirements analysis includes functional requirements analysis and non-functional requirements analysis. The high-level design includes system architecture design, functional module design, database design, and interface definition design. The detailed design includes the specific process implementation design for each functional module of the system. In addition, it provides a description and demonstration of the system's implementation, designs test cases for the system's implemented functions, and presents the test results.

**Keywords**：team collaboration development; retrieval-enhanced generation; Wiki generation

# 目 录

摘 要 I

Abstract II

目 录 III

第1章 绪 论 1

1.1 课题背景及研究的目的和意义 1

1.1.1 课题背景 1

1.1.2 研究目的和意义 1

1.2 检索增强生成的发展及其相关理论的发展概况 2

1.2.1 检索增强生成的发展 2

1.2.2 检索增强生成的核心理论基础 3

1.2.3 检索增强生成的前沿演进与自适应理论 4

1.3 大模型生成与SystemPrompt的发展及其相关理论的发展概况 4

1.3.1 大模型生成机制与提示词技术的发展 4

1.3.2 System Prompt控制生成的核心理论基础 5

1.3.3 复杂提示理论的演进与思维链的引入 5

1.4 本文的主要研究内容 6

第2章 智能代码问答系统需求分析 7

2.1 引言 7

2.2 系统功能性需求分析 7

2.2.1 系统用例描述 7

2.2.2 系统核心业务流程 9

2.3 系统非功能性需求分析 11

2.3.1 系统开发与运行环境 11

2.3.2 软件特性分析 12

2.4 本章小结 13

第3章 智能代码问答系统概要设计 14

3.1 引言 14

3.2 系统架构设计 14

3.3 功能模块设计 16

3.3.1 用户认证模块 16

3.3.2 仓库管理模块 17

3.3.3 Wiki生成模块 18

3.3.4 智能问答模块 19

3.3.5 深度研究模块 20

3.3.6 对话管理模块 21

3.3.7 用户设置模块 22

3.4 数据库设计 23

3.4.1 数据库实体关系设计 23

3.4.2 数据库概念模型设计 24

3.5 接口定义设计 27

3.6 本章小结 29

第4章 智能代码问答系统的详细设计与实现 30

4.1 引言 30

4.2 智能代码问答系统功能模块详细设计 30

4.2.1 用户认证模块详细设计 30

4.2.2 仓库管理模块详细设计 35

4.2.3 Wiki生成模块详细设计 37

4.2.4 智能问答模块详细设计 39

4.2.5 深度研究模块详细设计 40

4.2.6 对话管理模块详细设计 41

4.2.7 用户设置模块详细设计 43

4.3 智能代码问答系统功能模块功能实现 44

4.3.1 用户认证模块功能实现 44

4.3.2 仓库管理模块功能实现 46

4.3.3 Wiki生成模块功能实现 48

4.3.4 智能问答模块功能实现 51

4.3.5 深度研究模块功能实现 53

4.3.6 对话管理模块功能实现 55

4.3.7 用户设置模块功能实现 59

4.4 本章小结 60

第5章 智能代码问答系统的测试 61

5.1 引言 61

5.2 功能模块测试 61

5.2.1 用户认证模块功能测试 61

5.2.2 仓库管理模块功能测试 62

5.2.3 Wiki生成模块功能测试 64

5.2.4 智能问答模块功能测试 65

5.2.5 深度研究模块功能测试 66

5.2.6 对话管理模块功能测试 67

5.2.7 用户设置模块功能测试 68

5.3 本章小结 69

结 论 70

参考文献 71

攻读学士学位期间取得创新性成果 72

哈尔滨工业大学本科毕业论文（设计） 73

原创性声明和使用权限 73

致 谢 74

# 绪 论

## 课题背景及研究的目的和意义

### 课题背景

随着软件系统规模与复杂度持续提高，文档滞后与知识碎片化问题已成为软件开发中的普遍痛点。一方面在软件开发过程中跨语言、跨模块、跨仓库协作变为常态，代码面临着更迭频繁从而导致文档与代码不同步；另一方面，开发者在进行跨文件的问题定位、依赖关系梳理与调用链梳理过程中成本高、效率低，严重影响知识传承与协同开发。

近年来，检索增强生成（Retrieval-Augmented Generation,RAG）在知识密集型任务中被证明可以显著降低幻觉并提升事实一致性，为"先定位证据、再生成答案"的范式提供了基础。向量数据库作为 RAG 架构的核心组件，可以通过相似度检索支持大规模的知识库的实时查询。同时在代码层面，使用以函或语法数结构作为边界进行切分的方法可以利于保持语义完整性。Tree-sitter作为增量式语法解析器，支持多语言代码的高效解析与抽象语法树构建，为函数级的切分提供了技术基础。在推理层面，大语言模型的发展为智能问答系统提供问答基础。

基于上述条件，本项目聚焦于使用tree-sitter进行函数级的语义切割；将切片进行向量化并存入Qdrant向量数据库；通过定时任务与增量索引保证文档与代码的一致性；并设计多轮推理实现"定位—检索—聚合—生成"的闭环，输出绑定函数签名、文件路径知识产物。

### 研究目的和意义

本课题旨在构建一个基于检索增强生成（RAG）技术的代码知识智能问答系统。针对大型软件项目中的文档滞后、知识碎片化、跨文件理解成本高等问题，实现代码仓库知识的自动提取、持续更新与高质量问答，从而提升开发者对代码理解的效率，降低开发者间进行知识传承的成本，同时增强开发者的跨团队协作能力。

（1）构建出基于函数级切分的代码知识库：研发基于tree-sitter的多语言代码语法解析与函数级语义切分模块，实现自动提取函数的函数签名、参数列表、返回类型、起止行号、文档注释等完整元数据。通过Embedding嵌入模型将代码片段进行向量化，存入向量数据库，建立支持多仓库、多语言，可按元数据过滤、能够进行高效检索的知识库。

（2）实现索引与版本的一致性：设计并实现基于定时任务的仓库监听与增量索引更新功能，通过周期性执行git pull并解析变更文件列表（新增、修改、删除），触发更新流程，仅对受影响文件重新解析与向量化，未变更文件保持不变。增量索引机制可以确保知识库与代码版本实时同步，避免因文档滞后造成的信息失真与误导。

（3）实现检索增强的智能问答系统：设计用户查询的向量化与检索流程，将自然语言查询转换为向量后在Qdrant中执行相似度检索，召回高相关函数片段作为证据集。结合结构化提示模板与限定大模型输出格式，确保答案可验证、可回溯，有效降低幻觉答案比例，提升答案与事实的一致性。

## 检索增强生成的发展及其相关理论的发展概况

大语言模型（LLM）在很多自然语言任务里表现得很好，但由于其高度依赖静态参数的内部编码的知识。这就带来了两个明显的问题：一是模型没法实时更新知识，二是容易在内部知识不涉及的地方产生事实性错误，产生“幻觉”现象。为了解决这个问题，在此背景下，研究者尝试把信息检索技术和大模型相结合，让模型在生成回答之前先去外部数据库查资料。这就是检索增强生成（RAG）技术的起源。

### 检索增强生成的发展

检索增强生成的概念最初是为了解决开放域问答任务中的局限性。在早期的预训练语言模型中，系统通常只能靠预训练阶段记住的信息来硬答，这就导致了在面对知识密集型任务时，往往依赖于自身的参数化记忆。为了解决这一问题，2020年，Guu等人在国际机器学习大会（ICML）上提出了REALM模型\[1\]。该研究尝试在模型的预训练阶段引入知识检索机制，使模型在填词预测时能够借助维基百科等外部文档。为后续的检索增强框架奠定了初步基础。随后，Lewis等人发表在NeurIPS 2020上的研究正式把这种机制命名为“检索增强生成”\[2\]。在论文中，研究团队直接将预训练的序列到序列（Seq2Seq）模型，跟一种基于向量的检索工具连在一起进行训练。实验证明，这种让模型带着参考资料去生成答案的方法，在需要大量外部知识的测试中的效果明显优于先前的模型。从这篇工作之后，RAG开始成为自然语言处理中活跃的研究方向。

### 检索增强生成的核心理论基础

检索增强生成主要建立在信息检索理论和条件文本生成理论这两个基础上。

在信息检索理论方面，先前常用的BM25基于字面匹配的方法。但字面匹配无法处理处理语义相似但用词不同的情况。对此Karpukhin等人在EMNLP 2020会议上提出了DPR（Dense Passage Retrieval）模型\[3\]。DPR的核心理论是用双塔结构的神经网络，把用户的问题与知识库中的文档分别转换成低维稠密向量，然后通过计算向量之间的距离来判断内容的相关性。这个理论解决了语义检索的难题，也是现在大部分RAG系统都在用的基础架构。传统方法长期依赖于基于词频统计的稀疏检索，这种方法难以捕捉词汇背后的深层语义关联。

在生成理论方面，条件文本生成起到了关键作用。模型拿到检索出来的几段文档后，需要利用注意力机制（Attention）去判断哪些内容对回答当前问题真正有用。解码器在生成每一个词的时候，都会动态分配权重，把外部知识和用户原来的问题融合在一起，从而生成连贯的回答。

在条件文本生成理论方面，模型在获取到相关的外部文档片段后，需要利用注意力机制（Attention）去判断哪些内容对回答当前问题真正有用。目前的生成理论要求生成器在自回归解码的过程中，不但关注历史生成的上下文，同时将注意力分配给检索到的外部输入。把外部知识和用户原来的问题融合在一起，从而生成连贯的回答，确保最终生成的文本既符合自然语言逻辑，又符合检索到的事实依据。

### 检索增强生成的前沿演进与自适应理论

随着RAG技术的广泛应用，传统的“先检索，后生成”流程暴露出了一些缺陷。因为并不是所有的提问都需要检索外部资料，并且无差别的检索会引入无关噪声，反而对生成质量造成负面干扰的缺陷，导致回答质量下降。

针对这些实际问题，目前的理论开始往自适应和动态控制的方向发展。例如Asai等人在ICLR 2024会议上提出了Self-RAG机制\[4\]。这项研究引入了一种“反思标记”（Reflection Tokens）的理论。让模型自己去判断当前到底需不需要去检索资料。若发起检索，进行自我评估，判断检索的资料靠不靠谱，最后生成的答案有没有脱离事实。这种结合了自我评价的生成理论，使得RAG系统变得更加可控，这也是目前该领域前沿的理论演进方向。

## 大模型生成与SystemPrompt的发展及其相关理论的发展概况

大模型在生成文本的时候，并不是随意发挥的。目前的语言模型通常需要用户给出明确的指令或设定，尤其是System Prompt，它就像是给模型定下了一个全局的“人设”和行为规范。理解这部分技术的发展，对研究大模型怎么受控生成内容非常重要。

### 大模型生成机制与提示词技术的发展

早期的语言模型在进行生成时，主要依靠上下文来进行。2020年，Brown等人在神经信息处理系统大会（NeurIPS）上发表了关于GPT-3的经典研究\[1\]。在这篇文献里，他们提出了“上下文学习”（In-Context Learning）的理论。不需要去改动模型的底层参数，只要在输入的时候给几个例子或者加一句提示，模型就能学会按照要求的格式生成文本。这是提示词技术最早的形态。

随着大模型应用到具体的对话产品里，研究人员发现仅仅依靠提示词是不够稳定的。为了让模型在整个多轮对话里都保持稳定的表现，开发者开始在用户输入之前，在系统底层强制插入一段隐藏的指令。这就是System Prompt的由来。它从最初简单的“你是一个有用的助手”，慢慢演变成了现在包含角色设定、回答边界、输出格式等几十上百字的复杂说明书。

### System Prompt控制生成的核心理论基础

模型之所以在获取系统提示词后，就按照要求输出，是因为有指令微调（Instruction Tuning）理论。如果没有经过微调，预训练模型只会单纯地预测下一个词，当输入System Prompt后，它可能会顺着Prompt接着续写下去，而不是去执行它。为了解决这个问题，Ouyang等人在NeurIPS 2022会议上提出了InstructGPT模型及基于人类反馈的强化学习（RLHF）理论\[2\]。这项研究证明了，通过收集人类对不同回答的偏好进行训练，可以强行扭转大模型原有的生成概率分布。

从条件文本生成的理论角度来看，System Prompt实际上充当了一个全局的上下文条件。在模型解码生成每一个词的过程中，这段系统指令都在默默发挥作用。它会把那些不符合角色设定或者违反安全规则的词汇的生成概率压低，从而在数学层面上保证了最终生成的文本是受控的。

### 复杂提示理论的演进与思维链的引入

随着对System Prompt的研究，它的功能已经不只是限制语气或者设定角色，而是开始向引导模型进行复杂推理的方向演进。

在大模型早期，如果遇到复杂的逻辑问题，就算System Prompt写得再详细，模型也容易算错或者发生幻觉。后来，Wei等人在NeurIPS 2022上提出了著名的“思维链”（Chain of Thought, CoT）理论\[3\]。他们发现，只要在提示词里引导模型一步一步地思考，模型的推理准确率就会大幅度提升。目前，很多高级System Prompt，其实都融合了思维链理论。开发者会在系统提示词里提前写好拆解任务的框架，强迫模型在生成最终答案前，先在后台把思考的过程写出来。这种从“单纯设定规则”到“规范思考路径”的理论演变，极大地提高了大模型生成内容的逻辑性和可靠性。

## 本文的主要研究内容

本文的研究内容主要为基于检索增强生成技术的智能代码问答与管理系统。与传统的单纯依靠关键字搜索代码片段不同，本系统的侧重点在对代码库的深度语义理解和智能交互上，旨在打造一个以开发者日常编码场景为核心的辅助系统。在代码知识库的管理、语法检索、复杂业务逻辑的解答等方面，该系统能为程序员提供更顺手的工具，降低查阅分散文档的成本，有助于他们更高效地定位技术细节并提升整体开发效率。

在技术架构上，前端用户界面主要使用TypeScript进行逻辑开发，配合CSS完成页面渲染，前后端通过接口进行数据交互。后端的核心业务逻辑，使用Python语言来实现。数据存储在MySql、Qdrant数据库中

本文的组织结构如下：

第 1 章为绪论，主要阐释课题的研发背景，探讨开发这个智能代码系统的目的和实际意义。同时对国内外在代码语义检索、大模型辅助编程等相关领域的现状进行讨论和分析，最后概括本文的主要研究内容以及整体的行文结构。

第 2 章为智能代码系统需求分析，主要内容是对整个代码问答与管理系统进行全面的需求拆解。在进行分析时，重点关注功能性需求和非功能性需求两个方面，以确保系统在满足开发者期望的同时具备良好的运行性能。

第 3 章为智能代码系统概要设计，主要是确立系统的整体技术架构设计。这部分对系统后台的数据管理端和前端的大模型交互端进行了模块划分，同时对各个功能模块的数据库表结构以及前后端连接的API接口进行了统一定义。

第 4 章为智能代码系统详细设计与实现，分别对前台的用户交互模块和后端的RAG检索处理模块进行流程层面的详细设计。随后结合之前两章已经确定的需求和概要框架，具体阐述Python后端算法模块、TypeScript前端界面，最终完成整个系统的对接。

第 5 章为智能代码系统的测试，主要是对开发完成的代码辅助系统进行检验。在这一阶段，会对系统的各个模块进行全面的功能测试，包括设计针对不同功能点的测试用例并执行，以此来验证系统在各种使用场景下是否都能够正常运行并达到预期的设计指标。

# 智能代码问答系统需求分析

## 引言

本章主要内容是对智能代码问答系统进行全面的需求分析与设计，在进行分析时，主要关注系统功能性需求分析和非功能性需求分析两个部分，以确保系统能在满足用户期望的同时具备良好的性能。对系统功能性需求分析中提供系统用例图和核心业务流程图，基于不同的面向对象深入分析系统需求，在用例分析基础上设计系统核心业务，明确开发任务。对系统非功能性需求包括系统开发运行环境分析和软件特性分析，确定系统运行环境以及分析系统的功能性、安全性等软件特性，确保系统具备良好的性能和用户体验。

## 系统功能性需求分析

本系统主要面向程序员群体，该群体为本系统的唯一用户，同时兼顾后台管理员的身份，他们可以对系统的某些参数进行配置；程序员作为系统的用户，要求系统必须能够满足从程序员视角出发的浏览和问答等功能需求。这就要求在设计系统时需要从程序员的角度出发，把握面向对象的需求和期望，对系统的功能进行需求分析和设计。以下将详细介绍对系统的需求分析内容。

### 系统用例描述

本系统面向所有注册用户开放，用户注册登录后即可完整使用系统的全部功能，包括仓库管理、知识检索、问答交互与系统配置。系统采用"一人一账号"的自主管理模式，每个用户在自己的账号下独立管理仓库、配置参数与查看历史记录，无需管理员审批与权限分配。

用户即系统的唯一使用角色，涵盖开发者、运维人员、技术负责人等所有需要进行代码理解与知识检索的人员。用户注册登录后，可以在自己的账号下添加、删除或更新代码仓库，语言过滤规则与定时同步策略（设置定时任务周期，如每5分钟、每小时等）；可以查看已接入仓库的代码知识库信息，包括仓库名称、Wiki信息等数据；可以通过自然语言输入查询问题，系统自动进行向量检索并返回高相关的函数级代码片段作为证据，同时生成结构化答案并附带文件路径、行号等可追溯引用；用户可以跳转到源文件对应位置进行验证，也可以对答案进行追问、澄清或纠错；用户可以手动触发全量重建或增量索引任务；用户可以配置系统参数，包括向量化模型选择、生成模型选择、检索Top-k值、提示模板编辑、相似度阈值等；用户可以查看自己的查询历史、反馈记录等;用户可以对检索结果与生成答案进行反馈。具体的系统用例图如图2-1所示。

图2-1系统用例图

用例交互流程说明：

用户注册与登录流程步骤如下：（1）新用户访问系统，进入注册页面填写用户名、邮箱与密码；（2）系统验证信息并创建账号；（3）用户使用账号密码登录系统；（4）系统验证身份并分配会话令牌；（5）用户进入个人工作台。

仓库管理与知识库构建流程步骤如下：（1）用户登录后进入仓库管理界面，点击"添加仓库"，输入Git URL（GitHub或本地）；（2）系统自动执行git clone拉取仓库并触发首次全量索引；（3）系统使用tree-sitter按函数级切分代码，生成向量并写入Qdrant；（4）用户查看索引进度与解析日志；（5）索引完成后，系统定时任务定期执行git pull检查仓库更新；（6）系统根据git pull返回的变更信息（新增、修改、删除的文件列表）触发增量索引，仅对变更文件重新解析与向量化；（7）用户可在同步日志中查看最新拉取时间、变更文件数、增量索引耗时；（8）用户可随时手动触发全量重建或增量索引，或删除不再需要的仓库。

知识检索与问答流程步骤如下：（1）用户在工作台选择目标仓库，输入自然语言查询（如"用户认证流程如何实现"、"支付模块依赖哪些服务"）；（2）系统向量化查询并在Qdrant中检索Top-k高相关函数片段；（3）系统将证据集与结构化提示结合，调用生成模型生成答案；（4）用户查看答案与证据引用（文件路径、行号）；（5）用户可以跳转到源文件对应行进行验证；（6）用户对答案进行反馈（点赞表示满意，或标记错误并补充说明）；（7）系统记录反馈并更新质量评分；（8）用户可在查询历史中查看所有历史问答记录与反馈。

系统配置与个性化流程步骤如下：（1）用户进入系统配置界面，选择嵌入模型与生成模型；（2）调整检索Top-k值（如从5调整到10）；（3）保存配置并应用到后续查询。

### 系统核心业务流程

本系统的功能主要可以分为仓库接入与清洗、函数级代码切分、向量化与知识库构建、增量索引与自动更新、检索增强问答、多轮智能推理。

仓库接入与清洗功能支持通过Git URL自动拉取目标仓库，并对二进制文件、编译产物、第三方依赖目录等进行过滤，形成可解析源码集合。系统自动识别代码语言并记录文件路径等元数据，确保后续检索与答案可追溯到具体文件位置。

函数级代码切分功能采用tree-sitter语法解析器，按函数、类、方法等结构对源文件进行结构化切分，保留函数签名、参数列表、起止行号、文档注释等完整元数据。与固定窗口切分相比，函数级的颗粒度能够保持语义完整性，避免切分结果的语义断裂。

向量化与知识库构建功能将切分获得的函数级代码片段输入向量化模型，生成向量表示，并将向量及其元数据（文件路径、函数名、行号、语言类型、分支名）写入Qdrant向量数据库。

增量索引与自动更新功能面向代码持续更新的场景，系统通过定时任务对仓库进行git pull，自动触发增量更新：拉取变更文件列表，对新增、修改的文件重新解析与向量化，对删除的文件从Qdrant中移除对应向量，对未变更文件保持索引不变。增量策略保证文档、代码、版本的一致性，适配高频更新与多分支并行开发的场景。

检索增强问答功能，用户输入自然语言问题后，系统将用户问题向量化并在Qdrant中执行相似度检索，结合元数据过滤召回Top-k高相关函数片段作为证据集。系统将证据集与结构化System Prompt结合，限定大模型输出格式，降低幻觉并提升事实一致性。用户可自行跳转到源文件的对应行进行检查。

多轮智能推理功能面向复杂问题，系统采用"检索—推理—验证"循环：对用户问题进行回答，并交由大模型判断回答效果，若不足，则生成Next Question，继续迭代；针对每个子问题执行检索，汇聚证据片段，并生成结构化答案。

具体的功能流程与架构图如图2-2所示。

图2-2 系统架构与流程图

## 系统非功能性需求分析

### 系统开发与运行环境

系统开发和运行环境为笔记本电脑，处理器为Intel i7-12700H、NVIDIA GeForce RTX 3050，内存16G，64位 Windows11操作系统。系统编程开发工具为Visual Studio Code。后端使用 Python 语言开发，运行环境为Python 3.10版本。前端的页面交互与逻辑主要基于TypeScript语言编写，并搭配CSS完成页面渲染。数据库为向量数据库Qdrant与传统关系型数据库MySQL数据库，通过DBeaver进行事务性操作。

### 软件特性分析

从功能性角度来看，本系统核心围绕代码知识库的构建展开。主要具备了代码仓库或本地仓库的授权导入、AI自动化生成Wiki文档、文档在线预览以及基于RAG的代码智能问答等基础功能。整体的操作链路比较完整，用户在提交生成任务或进行对话时，系统都能给予准确的状态反馈。此外，在设计过程中，系统对用户的操作模块进行了明确的区分。用户的后台的模型配置页面与前台的文档查阅页面是独立实现的，互相不干扰，以便于针对不同角色进行精细化管理。

在易用性方面，本系统各个模块功能实现界面清晰简洁，并伴有必要的提示信息，便于用户理解和使用，降低用户的使用门槛。如当用户绑定私人仓库时，如果Token格式输入有误，系统会直接抛出明确的修改提示；当庞大的Wiki文档解析和生成完毕后，页面会自动出现状态改变的通知。考虑到数据安全，如果用户误点到了“删除知识库”这类高危按钮，系统会强制弹出一个二次确认的对话框，以防止误删。同时，界面对一些关键流程节点做了视觉上的强化，比如将最重要的“生成Wiki”或者“向AI提问”按钮做了明显的颜色高亮，通过这些提示，配合清晰直观的界面设计，用户能够方便直观地使用系统。

从安全性角度出发，由于本系统需要读取用户的代码资产并将其向量化，所以在数据隔离上需要严谨。系统底层的数据库设计充分考虑了用户之间、仓库之间的关系，对用户之间的资产权限、用户间的操作权限都做了严格隔离。确保数据不会发生越权读取，避免出现A用户不小心看到B用户私有代码仓库Wiki的错乱问题。

从可扩展性角度出发，本系统各模块间功能相对独立，代码耦合度低，代码架构比较灵活，当需要新增功能时，一般不会造成原有功能受到影响发生错误的情况。

## 本章小结

本章围绕智能代码问答系统，从系统功能性需求分析和非功能性需求分析两个方面对系统进行了全面的分析。在系统功能性需求分析中，从用户角色出发进行了详细的需求分析，实现了用例描述，确定了软件功能模块内容和系统的核心业务流程。同时在非功能性需求分析中，对系统开发的硬件和软件需求进行了明确，并对软件特性作出了分析，为系统的顺利开发和有效运行奠定了坚实的基础。

# 智能代码问答系统概要设计

## 引言

本章主要内容为智能代码问答系统进行概要设计。在本章中对系统的技术架构设计、系统用户端的功能模块设计进行了详细的设计，此外，还对系统数据库进行了详细的设计，对系统数据进行分析后对数据库实体关系进行了分析与设计并对每个功能模块涉及的详细数据库表进行了设计，同时确定了前后端连接的接口定义设计。

## 系统架构设计

本项目采取前后端分离的方式进行开发。前端实现使用技术栈为Next.js框架+React+TypeScript+Tailwind CSS，后端主要采用Python FastAPI框架+SQLAlchemy ORM进行实现，前后端之间通过RESTful API与WebSocket进行数据交互，向量数据库使用Qdrant，关系型数据库使用MySQL。AI服务层采用DashScope（通义千问Qwen系列模型）提供大语言模型生成能力与文本嵌入能力。智能代码问答系统的技术架构如图3-1所示。

图 3-1 智能代码问答系统技术架构图

本系统采用分层架构设计，自上而下分为展示层、服务层、AI服务层和数据层四个层次，各层职责明确，通过清晰的接口进行交互。

展示层对应前端页面绘制，主要为Web端开发，用户可通过Chrome或Edge等主流浏览器访问。UI实现主要采用Next.js框架+React组件化开发方式实现。Next.js是一个基于React的全栈Web开发框架，支持服务端渲染（SSR）、静态站点生成（SSG）以及API路由等功能，能够提供优秀的首屏加载性能和搜索引擎优化（SEO）表现。React采用声明式的、组件化的编程方式，使开发者能够更高效地构建用户界面。在样式方面，项目采用Tailwind CSS原子化CSS框架，通过组合预定义的样式类快速构建界面，避免了传统CSS的样式冲突和维护困难的问题。此外，项目还集成了Mermaid 图表渲染组件，支持在Wiki页面中渲染架构图、流程图和时序图等可视化内容。

服务层对应后端实现，主要采用Python FastAPI框架+SQLAlchemy ORM。FastAPI是一个现代、高性能的Python Web框架，基于Starlette和Pydantic构建，支持异步请求处理、自动生成OpenAPI 文档和类型校验等特性，其性能可与Node.js和Go相媲美。SQLAlchemy是Python生态中最流行的ORM框架之一，提供了完整的SQL工具包和对象关系映射能力，支持异步数据库操作，为系统的数据持久化提供了坚实的基础。服务层通过RESTful API接口接收前端请求，通过WebSocket协议实现实时的AI对话流式响应。

AI 服务层负责大语言模型调用与文本嵌入服务。本项目采用阿里云DashScope平台提供的通义千问Qwen系列模型作为核心AI引擎，具体支持qwen3.6-max-preview、qwen3.6-plus、qwen3.6-flash等多种规格的大语言模型用于代码问答和Wiki文档生成，同时采用tongyi-embedding-vision-plus模型提供文本嵌入服务，将代码片段转化为高维向量用于语义检索。AI服务层通过适配器模式对模型调用进行了统一封装，使系统具备良好的可扩展性，未来可便捷地接入其他模型提供商。

数据层采用双重存储架构。关系型数据使用MySQL数据库进行存储，MySQL是最流行的关系型数据库管理系统之一，具有高性能、高可靠性和易用性等优点，负责存储用户账户信息、用户配置偏好、对话历史记录和消息内容等结构化数据。向量数据使用Qdrant向量数据库进行存储，Qdrant是一个高性能的向量相似度搜索引擎，采用Rust语言编写，支持cosine距离、欧氏距离等多种相似度度量方式，负责存储代码仓库的文本嵌入向量，为RAG（检索增强生成）检索提供高效的语义搜索能力。

## 功能模块设计

智能代码问答系统的功能主要面向用户端，包括用户认证模块、仓库管理模块、Wiki 生成模块、智能问答模块、深度研究模块、对话管理模块和用户设置模块七个功能模块，每个模块包含不同的功能实现。具体的智能代码问答系统功能模块设计图如图3-2所示。

图 3-2 智能代码问答系统功能模块设计图

### 用户认证模块

用户认证模块负责系统的用户注册、登录和身份验证功能，是整个系统的安全入口。具体的用户认证模块设计图如图3-3所示。

图 3-3 用户认证模块设计图

在用户注册方面，系统要求提供用户名、电子邮箱和密码，注册时对用户名和邮箱进行唯一性校验。密码通过passlib库使用PBKDF2-SHA256算法进行哈希处理后存储于users表的password_hash字段，系统中任何环节不存储明文密码。在用户登录方面，系统验证用户名和密码后颁发JWT访问令牌，采用HS256算法签名，Token包含用户ID、签发时间和过期时间，默认有效期为60分钟。

### 仓库管理模块

仓库管理模块是系统的入口模块，提供代码仓库的输入解析、类型识别和管理功能。具体的仓库管理模块设计图如图3-4所示。

图 3-4 仓库管理模块设计图

在输入解析方面，系统支持三种格式：GitHub仓库URL（自动解析出owner和repo名称）、本地文件系统路径（Windows或Unix绝对路径，类型设为local）和自定义Git仓库地址。在文件过滤配置方面，系统提供排除模式（默认忽略虚拟环境、版本控制、构建输出、缓存目录等）和包含模式（仅处理指定目录和文件），过滤器配置通过localStorage以仓库URL为键进行缓存。此外，该模块还支持配置GitHub Personal Access Token以访问私有仓库，并提供了历史项目列表展示功能，用户可快速访问之前分析过的仓库。

### Wiki生成模块

Wiki生成模块是系统的核心功能之一，利用AI大语言模型自动分析代码仓库并生成结构化的Wiki文档。该模块涉及代码拉取、代码拆分、向量嵌入、文档生成和缓存管理五个子流程。具体的Wiki生成模块设计图如图3-5所示。

图 3-5 Wiki生成模块设计图

在代码拉取阶段，系统首先检查本地是否已存在仓库目录——若存在且非空则直接复用避免重复克隆；否则使用git clone命令执行浅克隆以节省存储空间和网络带宽。对于本地类型仓库，系统通过接口读取本地文件系统的目录树和README文件内容。

在代码拆分阶段，系统使用Tree-sitter代码拆分器对代码文件进行语义级切分。Tree-sitter是一个增量解析库，能够将源代码解析为AST（抽象语法树）并根据语法节点精确提取函数、类、方法等代码单元。每个切分后的代码块以CodeChunk数据类存储，包含文本内容、文件路径、编程语言、块类型、函数名、类名、起止行号等元数据。对于不支持的文本文件类型（如Markdown、配置文件），系统回退到固定长度的字符切分方式（split_text_fixed）。

在向量嵌入阶段，系统调用embedding模型将切分后的文本块转换为1024维的稠密向量。嵌入过程以批量模式执行。生成的向量连同文本块的元数据一起存储到Qdrant向量数据库，每个代码仓库拥有独立的集合，向量相似度度量采用余弦距离。

在文档生成阶段，AI模型基于向量检索到的代码上下文生成包含架构概览、模块功能说明、组件依赖关系等内容的Wiki文档。生成的Wiki数据以JSON格式缓存到服务器文件系统，支持按用户和语言进行隔离存储。用户可将Wiki导出为Markdown或JSON格式文件下载。

此外，系统还需要实现增量索引刷新功能。该模块定期对已克隆的仓库执行git pull拉取最新变更，通过git diff识别新增（A）、修改（M）和删除（D）的文件，仅对发生变化的文件重新执行代码拆分和向量更新操作，避免了每次全量重建索引的计算开销。

### 智能问答模块

智能问答模块是系统交互的核心，采用RAG（检索增强生成）技术为用户提供精准的代码相关问答服务。具体的智能问答模块设计图如图3-6所示。

图 3-6 智能问答模块设计图

该模块通过WebSocket协议进行实时流式交互。当用户提问时，系统首先将问题转换为嵌入向量并在Qdrant中进行余弦相似度检索，获取语义最相关的代码片段作为上下文。然后构建包含系统提示词、历史对话、检索上下文、当前文件内容和用户问题的完整Prompt。最后通过DashscopeClient异步调用大语言模型进行流式生成，文本片段通过WebSocket实时推送到前端渲染。系统内置了Token超限降级机制，当上下文过长时自动移除检索上下文进行重试。

在检索阶段，前端发送包含用户问题的JSON请求。后端将用户问题通过相同的嵌入模型转换为查询向量，在Qdrant集合中执行余弦相似度检索，返回top_k个最相关的文档片段，每个结果包含文本内容及其元数据。

在上下文构建阶段，系统将检索到的文档按文件路径分组，每个文件的代码片段用分隔线连接，形成格式化的检索上下文。最终的Prompt由系统提示词、文件内容、检索上下文和用户问题组成，其中系统提示词指定回答语言，并且明确约束模型输出。

在流式生成阶段，后端获取provider对应的模型配置，构造API调用参数，通过DashscopeClient的异步调用接口发起流式请求。模型生成的文本片段通过WebSocket实时推送到前端，前端按字符级增量渲染展示。

### 深度研究模块

深度研究模块是在智能问答基础上的增强功能，采用服务端多轮迭代的研究模式对用户提出的复杂问题进行深入分析，最大迭代次数设为5轮。具体的深度研究模块设计图如图3-7所示。

图 3-7 深度研究模块设计图

深度研究模块由服务端控制。在每一轮迭代中，系统从当前的消息列表中提取最新的用户问题作为研究主题，确保所有迭代保持对同一问题的聚焦。然后执行与普通问答相同的RAG检索流程获取相关代码上下文，但使用专门的系统提示词引导模型输出分阶段的研究成果。提示词分为三种类型：第一轮引导模型首先输出"## Research Plan"说明研究规划和方法，然后提供初步发现，最后以"## Next Steps"指出下一轮研究方向；中间轮次引导模型基于前几轮的发现识别研究空白并进一步深入调查，输出格式为"## Research Update {迭代轮次}"；最后一轮引导模型综合前几轮的所有发现，以"## Final Conclusion"为标题输出包含代码引用和实现细节的完整结论。

每轮生成完成后，模型输出的内容会作为下一条assistant消息追加到对话上下文中，使后续轮次能够"看到"前几轮的研究成果。前端组件将多轮研究结果以分页形式展示，用户可在不同研究阶段之间通过翻页按钮切换浏览。

### 对话管理模块

对话管理模块负责用户与AI之间对话会话的全生命周期管理。具体的对话管理模块设计图如图3-8所示。

图 3-8 对话管理模块设计图

在对话会话管理方面，用户可请求获取指定仓库下的所有历史对话列表，结果按更新时间倒序排列；请求创建新的对话会话；请求删除指定对话。每次对话的创建和访问都需要验证当前用户对对话的所有权，所有数据库查询都同时匹配conversation_id和current_user.id，防止用户越权访问他人的对话。

在消息管理方面，用户可以获取指定对话的所有消息；创建新消息，支持区分普通消息（normal）和深度研究消息（deep_research）两种类型。

在消息反馈方面，用户可对AI回答进行"赞"或"踩"的评价并附文字反馈，反馈数据便于后续回答质量评估。

### 用户设置模块

用户设置模块允许用户个性化配置系统的运行参数。具体的用户设置模块设计图如图3-9所示。

图 3-9 用户设置模块设计图

用户的配置数据包含三个配置子模块：嵌入设置用于配置嵌入向量的维度参数，该维度值会直接影响Qdrant集合的向量维度和问答时的检索精度；检索设置用于配置RAG检索时返回的相关文档数量，增大此值可获取更多的上下文信息但会增加Token消耗；自动刷新设置用于控制增量索引功能的启用状态和刷新间隔。

## 数据库设计

### 数据库实体关系设计

根据对本系统的数据分析，数据库实体关系设计如图 3-5 所示。

图 3-10 数据库实体关系设计图

数据库共包含主要实体5个，分别为用户、用户配置、对话、对话消息和消息反馈。用户实体的属性包括用户ID、用户名、电子邮箱、密码哈希、激活状态、创建时间和更新时间，用户配置实体的属性包括配置ID、首选语言、偏好模型、主题、通知开关、扩展配置、创建时间和更新时间，对话实体的属性包括对话ID、仓库所有者、仓库名称、仓库类型、对话标题、创建时间和更新时间，对话消息实体的属性包括消息ID、消息角色、消息内容、Token数量、反馈状态、消息类型和创建时间，消息反馈实体的属性包括反馈ID、反馈类型和反馈备注。

各个实体间的关系包含一对一和一对多两种，具体如下：用户实体对用户配置实体为一对一关系，即一个用户对应一个个性化配置；用户实体对对话实体为一对多关系，即一个用户可以创建多个对话会话；对话实体对对话消息实体为一对多关系，即一个对话包含多条消息；对话消息实体对消息反馈实体为一对一关系，即一条消息可以有一个用户反馈评价。

### 数据库概念模型设计

通过对本系统的数据分析，确定所需要用的数据库表格共有5个，分别为用户账户表、用户配置表、对话历史表、对话消息表和消息反馈表，各个数据库表对应的英文表名和具体描述如表3-1所示。

表 3-1 数据库表目录

| **序号** | **英文表名** | **中文名称** |
| --- | --- | --- |
| 1   | users | 用户账户表 |
| 2   | user_settings | 用户配置表 |
| 3   | conversations | 对话历史表 |
| 4   | conversation_messages | 对话消息表 |
| 5   | message_feedbacks | 消息反馈表 |

以下分别对表 3-1 数据库表目录中提到的数据库表进行介绍。

用户账户表的作用是存储用户的基本认证信息，包含的字段名有id、username、email、password_hash、is_active、created_at、updated_at，其中字段名id为该表的主键。用户名和电子邮箱均设置为全局唯一约束，密码字段存储经过PBKDF2-SHA256算法哈希处理后的值，禁止明文存储。用户账户表的设计如表3-2所示。

表 3-2 数据库表users设计表

| **字段名** | **数据类型** | **约束** | **说明** |
| --- | --- | --- | --- |
| id  | CHAR(36) | PRIMARY KEY, DEFAULT (UUID()) | 用户唯一标识（UUID） |
| username | VARCHAR(64) | NOT NULL, UNIQUE | 用户名，全局唯一 |
| email | VARCHAR(255) | NOT NULL, UNIQUE | 电子邮箱地址，全局唯一 |
| password_hash | VARCHAR(255) | NOT NULL | 密码哈希值（PBKDF2-SHA256） |
| is_active | TINYINT(1) | NOT NULL, DEFAULT 1 | 账户是否启用 |
| created_at | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 账户创建时间 |
| updated_at | DATETIME | NOT NULL, ON UPDATE CURRENT_TIMESTAMP | 账户最后更新时间 |

用户配置表的作用是存储用户的个性化偏好设置，与用户账户表为一对一关联，包含的字段名有 id、user_id、preferred_language、preferred_model、theme、notifications_enabled、extra_config、created_at、updated_at，其中字段名 id 为该表的主键，user_id 为外键关联到 users 表。extra_config 字段采用 JSON 类型，用于存储嵌入维度、检索参数和自动刷新等扩展配置信息。用户配置表的设计如表 3-3 所示。

表 3-3 数据库表 user_settings 设计表

| **字段名** | **数据类型** | **约束** | **说明** |
| --- | --- | --- | --- |
| id  | CHAR(36) | PRIMARY KEY, DEFAULT (UUID()) | 配置唯一标识 |
| user_id | CHAR(36) | NOT NULL, UNIQUE, FK → users.id | 关联用户 ID |
| preferred_language | VARCHAR(16) | NOT NULL, DEFAULT 'zh' | 首选界面语言 |
| preferred_model | VARCHAR(128) | NULL | 偏好 AI 模型标识 |
| theme | VARCHAR(16) | NOT NULL, DEFAULT 'light' | UI 主题（light/dark） |
| notifications_enabled | TINYINT(1) | NOT NULL, DEFAULT 1 | 是否启用通知 |
| extra_config | JSON | NULL | 扩展配置（JSON 格式） |

对话历史表的作用是存储用户与 AI 模型的对话会话信息，包含的字段名有id、user_id、repo_owner、repo_name、repo_type、title、created_at、updated_at，其中字段名 id 为该表的主键，字段名 user_id 为外键关联到 users 表。每条对话记录与一个特定的代码仓库关联，通过 repo_owner 和 repo_name 字段标识。对话历史表的设计如表 3-4 所示。

表 3-4 数据库表 conversations 设计表

| **字段名** | **数据类型** | **约束** | **说明** |
| --- | --- | --- | --- |
| id  | CHAR(36) | PRIMARY KEY, DEFAULT (UUID()) | 对话唯一标识 |
| user_id | CHAR(36) | NOT NULL, FK → users.id | 发起对话的用户 ID |
| repo_owner | VARCHAR(255) | NOT NULL | 仓库所有者 |
| repo_name | VARCHAR(255) | NOT NULL | 仓库名称 |
| repo_type | VARCHAR(32) | NOT NULL, DEFAULT 'github' | 仓库类型 |
| title | VARCHAR(512) | NULL | 对话标题 |
| created_at | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 对话创建时间 |

对话消息表的作用是存储对话中每条具体消息的内容，包含的字段名有 id、conversation_id、role、content、token_count、feedback_status、message_type、created_at，其中字段名 id 为该表的主键，字段名 conversation_id 为外键关联到 conversations 表。role 字段通过 CHECK 约束限制只能为 'user' 或 'assistant'。message_type 字段区分普通对话消息（normal）和深度研究消息（deep_research）。对话消息表的设计如表 3-5 所示。

表 3-5 数据库表 conversation_messages 设计表

| **字段名** | **数据类型** | **约束** | **说明** |
| --- | --- | --- | --- |
| id  | CHAR(36) | PRIMARY KEY, DEFAULT (UUID()) | 消息唯一标识 |
| conversation_id | CHAR(36) | NOT NULL, FK → conversations.id | 所属对话 ID |
| role | VARCHAR(16) | NOT NULL, CHECK IN ('user','assistant') | 消息角色 |
| content | TEXT | NOT NULL | 消息正文内容 |
| token_count | INT | NULL | Token 数量 |
| feedback_status | VARCHAR(16) | NULL, CHECK IN ('liked','disliked') | 反馈状态 |
| message_type | VARCHAR(16) | NOT NULL, DEFAULT 'normal' | 消息类型 |

消息反馈表的作用是存储用户对 AI 回答消息的详细反馈信息，包含的字段名有 id、message_id、feedback_type、comment、created_at，其中字段名 id 为该表的主键，字段名 message_id 为外键关联到 conversation_messages 表。消息反馈表的设计如表 3-6 所示。

表 3-6 数据库表 message_feedbacks 设计表

| **字段名** | **数据类型** | **约束** | **说明** |
| --- | --- | --- | --- |
| id  | CHAR(36) | PRIMARY KEY, DEFAULT (UUID()) | 反馈唯一标识 |
| message_id | CHAR(36) | NOT NULL, FK → conversation_messages.id | 关联消息 ID |
| feedback_type | VARCHAR(16) | NOT NULL, CHECK IN ('liked','disliked') | 反馈类型 |
| comment | TEXT | NULL | 反馈备注文字 |
| created_at | DATETIME | NOT NULL, DEFAULT CURRENT_TIMESTAMP | 反馈创建时间 |
| id  | CHAR(36) | PRIMARY KEY, DEFAULT (UUID()) | 反馈唯一标识 |

所有数据库表均使用 InnoDB 存储引擎以支持外键约束和事务操作，字符集统一采用 utf8mb4（utf8mb4_unicode_ci 排序规则），确保完整支持 Unicode 字符（包括 Emoji）。所有外键约束均设置 ON DELETE CASCADE 级联删除策略，保证数据引用完整性。系统支持 MySQL 8.0+ 和 SQLite 两种数据库后端，通过 SQLAlchemy ORM 实现了数据库后端的透明切换，开发环境可使用 SQLite 文件数据库以降低环境搭建成本，生产环境推荐使用 MySQL 以获得更好的并发性能和可靠性。

## 接口定义设计

本系统前后端之间的接口通信采用两种模式：对于普通的 CRUD 操作，使用 RESTful API 进行请求-响应式交互；对于 AI 对话生成等需要实时流式响应的场景，使用 WebSocket 协议进行双向通信。以下对本系统涉及的主要接口进行定义说明。

本系统用户认证相关接口设计如表 3-7 所示。

表 3-7 智能代码问答系统用户认证接口设计表

| **序号** | **接口路径** | **请求方式** | **接口说明** | **认证要求** |
| --- | --- | --- | --- | --- |
| 1   | /auth/register | POST | 用户注册，创建新账户并返回 JWT 令牌 | 无需认证 |
| 2   | /auth/login | POST | 用户登录，验证用户名密码并返回 JWT 令牌 | 无需认证 |
| 3   | /auth/me | GET | 获取当前登录用户的个人信息 | Bearer Token |
| 1   | /auth/register | POST | 用户注册，创建新账户并返回 JWT 令牌 | 无需认证 |
| 2   | /auth/login | POST | 用户登录，验证用户名密码并返回 JWT 令牌 | 无需认证 |

本系统对话管理相关接口设计如表 3-8 所示。

表 3-8 智能代码问答系统对话管理接口设计表

| **序号** | **接口路径** | **请求方式** | **接口说明** | **认证要求** |
| --- | --- | --- | --- | --- |
| 1   | /api/conversations | GET | 获取指定仓库下的对话历史列表 | Bearer Token |
| 2   | /api/conversations | POST | 创建新的对话会话 | Bearer Token |
| 3   | /api/conversations/{id} | DELETE | 删除指定对话及其所有消息 | Bearer Token |
| 4   | /api/conversations/{id}/messages | GET | 获取指定对话的消息列表 | Bearer Token |
| 5   | /api/conversations/{id}/messages | POST | 在指定对话中创建新消息 | Bearer Token |

本系统 AI 问答与 Wiki 生成相关接口设计如表 3-9 所示。

表 3-9 智能代码问答系统 AI 问答与 Wiki 接口设计表

| **序号** | **接口路径** | **通信方式** | **接口说明** | **认证要求** |
| --- | --- | --- | --- | --- |
| 1   | /ws/chat | WebSocket | 实时流式 AI 代码问答（基于 RAG 检索增强） | 无需认证 |
| 2   | /ws/chat/deep-research | WebSocket | 多轮深度研究模式问答 | 无需认证 |
| 3   | /chat/completions/stream | POST | HTTP 流式 AI 问答（兼容端点） | 无需认证 |
| 4   | /api/wiki_cache | GET | 获取缓存的 Wiki 数据 | 无需认证 |
| 5   | /api/wiki_cache | POST | 保存生成的 Wiki 数据到缓存 | 无需认证 |

本系统辅助功能接口设计如表 3-10 所示。

表 3-10 智能代码问答系统辅助功能接口设计表

| **序号** | **接口路径** | **请求方式** | **接口说明** | **认证要求** |
| --- | --- | --- | --- | --- |
| 1   | /models/config | GET | 获取可用 AI 模型提供商及模型列表配置 | 无需认证 |
| 2   | /api/user/settings | GET | 获取当前用户的个性化配置 | Bearer Token |
| 3   | /api/user/settings | PUT | 更新当前用户的个性化配置 | Bearer Token |
| 4   | /api/processed_projects | GET | 获取已处理的仓库项目列表 | 无需认证 |
| 5   | /api/repos/refresh | POST | 手动触发增量索引刷新 | 无需认证 |

WebSocket 接口的通信流程为：前端首先建立 WebSocket 连接，连接成功后发送 JSON 格式的请求体（包含仓库 URL、对话消息列表、模型提供商、模型名称、语言设置、检索参数等信息），后端在接收到请求后执行代码检索和 RAG 增强流程，通过 WebSocket 连接持续推送生成的文本片段，前端实时渲染展示。对于深度研究模式，后端通过 @@PAGE|type|title@@ 标记分隔不同研究阶段的内容，前端据此分页展示研究报告。

## 本章小结

本章对智能代码问答系统进行了概要设计，完成了对系统开发所需的架构的设计——包括基于 Next.js + React 的前端展示层、基于 FastAPI + SQLAlchemy 的后端服务层、基于 DashScope 通义千问模型的 AI 服务层以及基于 MySQL + Qdrant 双重存储的数据层的四层架构；完成了对系统用户端功能模块的设计——包括用户认证模块、仓库管理模块、Wiki 生成模块、智能问答模块、深度研究模块、对话管理模块和用户设置模块七个核心功能模块的划分与功能定义；完成了数据库实体关系的设计——确定了用户、用户配置、对话、对话消息和消息反馈五个核心实体及其间的关联关系；完成了数据库概念模型的设计——包括用户账户表、用户配置表、对话历史表、对话消息表和消息反馈表共五张数据库表的详细结构设计。最后还根据功能模块进行具体功能分析，对前后端接口进行定义，完成了 RESTful API 和 WebSocket 共两大类 28 个接口的定义设计。概要设计着重分析设计了系统的功能与模块之间的交互，为接下来对系统的详细设计与实现打下基础。

# 智能代码问答系统的详细设计与实现

## 引言

本章将对智能代码问答系统进行详细设计与实现，分别对用户端的各个功能模块进行流程实现的详细设计，给出对应的流程图或者活动图辅助理解，然后结合详细设计内容和之前两章已完成的需求分析和概要设计部分，分别对用户端的各个功能模块进行实现和展示。

## 智能代码问答系统功能模块详细设计

智能代码问答系统功能模块包括用户认证模块、仓库管理模块、Wiki生成模块、智能问答模块、深度研究模块、对话管理模块、用户设置模块七个功能模块，下面将主要对上述提到的功能模块进行详细设计。

### 用户认证模块详细设计

用户认证模块包括用户注册、用户登录、Token验证和路由守卫四个主要功能。该模块在后端由auth_router.py实现核心认证逻辑，在前端由AuthContext上下文组件统一管理认证状态，配合Next.js中间件实现页面级路由拦截，是系统安全体系的第一道防线。

（1）用户注册

用户通过注册页面创建新账户。注册时用户需要填写用户名、电子邮箱、密码和确认密码四个字段。前端在用户提交表单时执行三层校验：首先检查所有字段是否非空，空字段则提示“所有字段均为必填项”；其次校验两次输入的密码是否一致，不一致则提示“两次输入的密码不一致”；最后检查密码长度是否满足至少 8 个字符的要求，不满足则提示“密码长度至少为 8 个字符”。三层校验全部通过后，前端调用AuthContext的register方法向POST /auth/register端点发送注册请求，请求体通过Pydantic RegisterRequest模型进行自动校验，其中username要求长度为3到64个字符，email要求符合电子邮箱格式，password要求至少 8 个字符。

后端在接收到注册请求后，首先通过一条SQL查询同时检查用户名和邮箱是否已被占用。如果用户名重复，返回HTTP 409状态码和“用户名已被占用”错误；如果邮箱重复，返回“该邮箱已注册”错误。均未占用的情况下，调用 \_hash_password() 函数将用户提交的原始密码通过PBKDF2-SHA256 算法进行哈希处理，该算法通过引入随机盐值和多轮迭代（默认29000轮）大幅增加了暴力破解的计算成本。哈希后的密码存入users表的password_hash字段，系统全程不存储明文字段。用户记录通过SQLAlchemy ORM写入数据库后，系统直接调用_create_access_token()函数签发JWT Token，即注册即登录，用户无需二次认证。JWT Token采用HS256对称签名算法，签名密钥通过环境变量 JWT_SECRET_KEY 注入，Payload中包含用户ID、签发时间和过期时间（exp），默认有效期为60分钟。

前端在接收到成功响应后，将用户信息（username和email）存为localStorage 的cw_user键，将JWT Token存为localStorage的cw_token键，同时设置浏览器Cookie cw_authed=1供Next.js中间件进行服务端路由判断，最后通过React Context更新全局用户状态并跳转至/dashboard仪表盘页面。用户注册的完整活动流程如图4-1所示。

图 4-1 用户注册流程图

（2）用户登录

用户通过登录页面进行身份认证。登录时用户需要填写用户名和密码，前端检查两者均非空后，调用AuthContext的\`login\`方法向\`POST /auth/login\`端点发送登录请求。

后端接收到登录请求后，以用户名为条件在\`users\`表中查询对应记录。若用户不存在或密码验证不通过，系统统一返回HTTP 401状态码和“用户名或密码错误”的提示信息，不区分“用户不存在”和“密码错误”两种情况，以防止攻击者通过错误信息差异枚举有效用户名。密码验证通过\`\_verify_password()\`函数完成，该函数调用passlib库的\`verify()\`方法将用户提交的明文与数据库中存储的PBKDF2-SHA256哈希值进行比对。密码验证通过后，系统额外检查\`is_active\` 字段，若该字段为\`False\`，返回 HTTP 403 状态码和“账户已停用”错误提示。全部验证通过后，系统签发JWT Token并返回用户信息，前端执行与注册成功后相同的状态持久化流程并跳转至仪表盘页面。用户登录的完整活动流程如图4-2所示。

图 4-2 用户登录流程图

（3）JWT Token验证与会话保持

所有需要认证的后端接口均通过FastAPI依赖注入机制调用get_current_user函数进行统一的Token验证。该函数从请求头中提取Bearer Token，调用函数使用HS256算法和JWT_SECRET_KEY密钥对Token进行解码和验签，自动校验签名有效性和过期时间。解码成功后提取sub字段作为用户ID，在数据库中查询对应记录。若Token缺失、签名无效、Token过期或用户不存在、账户已停用，均抛出HTTP 401异常，向客户端返回相应的中文错误提示。

前端在应用初始化时，从localStorage中读取已保存的cw_user和cw_token恢复用户登录状态，随后在后台异步调用GET /auth/me端点验证Token是否仍然有效。若后端返回非2xx状态码则表示Token已过期或无效，前端清除所有本地认证数据并将用户状态置为未登录；JWT Token的完整验证活动流程如图4-3所示。

图 4-3 JWT Token验证流程图

（4）路由守卫

系统采用双层路由守卫策略。在Next.js中间件层，每次页面请求到达服务端渲染之前执行。中间件定义了受保护路由的前缀（/dashboard、/wiki、/user）和认证专用路由（/login、/regiter），通过检查请求是否携带cw_authed Cookie判断用户登录状态。当未登录用户访问受保护路由时，中间件将其重定向至/login页面；当已登录用户访问/login或/register时，中间件将其重定向至 /dashboard。在API层，get_current_user函数为所有受限接口提供JWT Token校验。路由守卫的决策活动流程如图 4-4 所示。

图 4-4 路由守卫流程图

### 仓库管理模块详细设计

仓库管理模块是系统的入口模块，包括仓库输入解析、仓库类型识别、文件过滤器配置、访问令牌管理、历史项目列表五个主要功能。

（1）仓库输入解析与类型识别

系统支持三种格式的仓库输入：GitHub仓库URL、本地文件系统路径和自定义Git仓库地址。用户进入Dashboard页面后，在输入框中输入仓库地址并点击“生成Wiki”按钮，系统即调用函数对输入内容进行解析和类型识别。

解析逻辑按照以下顺序进行：首先使用Windows路径正则表达式匹配形如 C:\\Users\\xxx\\project的绝对路径，匹配成功则将类型标记为local，仓库名称取路径的最后一级目录名，所有者设为local；其次判断是否以/开头，即Unix绝对路径，同样标记为local类型；最后使用Git URL正则表达式匹配远程仓库地址，匹配成功后根据域名判断仓库类型，域名为github.com则标记为github，其他域名则标记为web，同时从URL路径中提取仓库所有者（owner）和仓库名称（repo），并自动去除.git后缀。

对于 GitHub 仓库，系统支持配置个人访问令牌以访问私有仓库。用户可在配置弹窗中展开Token设置区域，粘贴自己的GitHub Token。Token会随仓库URL一同传递到后端，后端在克隆仓库时将Token嵌入Git URL。

（2）文件过滤器配置

在配置弹窗中，用户可以通过文件过滤器控制哪些目录和文件参与Wiki生成和问答检索。过滤器分为两种模式：排除模式（Exclude）和包含模式（Include）。排除模式下，用户指定的目录和文件将被排除在处理范围之外；包含模式下，只有用户指定的目录和文件才会被处理。

系统内置了默认的排除目录列表和排除文件列表，这些默认值在前端组件中以常量形式定义，在后端配置文件中同样维护。默认排除的目录包括虚拟环境目录、依赖目录、版本控制目录、构建输出目录、Python 缓存目录、IDE 配置目录和临时目录等。默认排除的文件包括包管理器锁文件、系统文件、配置文件、二进制文件和媒体文件等。用户可在默认排除项的基础上追加自定义排除规则，每行填写一个目录路径或文件名模式。

（3）Wiki生成启动与历史项目

用户点击“生成Wiki”按钮后，系统首先对仓库输入进行解析校验。若解析失败，则在输入框下方显示错误提示“Invalid repository format”。校验通过后弹出ConfigurationModal配置弹窗，用户可在弹窗中调整AI模型选择、语言设置、Wiki类型、文件过滤器和访问令牌等参数。确认所有配置后点击提交，系统将当前仓库的所有配置参数序列化存localStorage的deepwikiRepoConfigCache键中，然后通过Next.js路由跳转至动态页面，同时将所有配置参数通过URL查询字符串传递，进入Wiki生成的实际处理流程。

在Dashboard页面的下半部分，系统通过组件展示用户已处理过的历史项目列表。该列表数据来自后端/api/wiki/projects接口，每个项目展示仓库名称、所有者、仓库类型和处理时间等信息。用户可直接点击历史项目快速进入对应仓库的Wiki页面，也可通过搜索框按项目名称或仓库名关键词进行筛选。此外，每个历史项目右侧提供了删除按钮，用户确认后向后端发送DELETE请求删除该项目在服务端的所有缓存数据。

仓库管理模块的完整活动流程如图4-5所示。

图 4-5 仓库管理模块流程图

### Wiki生成模块详细设计

Wiki生成模块是系统的核心功能模块，利用AI大语言模型自动分析代码仓库并生成结构化Wiki文档。该模块涉及代码拉取、语义拆分、向量嵌入、AI文档生成和增量索引五个关键环节。

（1）代码获取与预处理

仓库下载首先检查Git环境可用性，然后检查本地是否已有仓库缓存，已有则直接复用。若无缓存，执行git clone进行克隆，仅获取最新提交内容。若提供访问令牌，将其嵌入克隆URL用于访问私有仓库。

仓库下载后存储于~/.adalflow/repos/{owner}\_{repo_name}/目录下。文件读取支持排除模式和包含模式两种过滤策略。排除模式下过滤掉默认排除目录和用户自定义排除项；包含模式下仅处理用户指定的目录和文件。

（2）语义代码拆分

代码拆分由Tree-sitter进行AST级别的语义切分。与固定长度切分相比，语义切分能按函数、类、方法等单元精确切分，保持代码逻辑完整性。系统支持Python、JavaScript、TypeScript、Java、Go、Rust、C/C++、C#等多种语言，每种语言配置对应的AST节点类型。对于Markdown、JSON等非代码文件，降级为固定长度切分。每个切分后的代码块附带元数据，包括文件路径、函数名、类名、起止行号等。

（3）向量嵌入与Qdrant索引

向量嵌入采用DashScope平台的tongyi-embedding-vision-plus模型，默认输出1024维向量。输出维度优先使用用户配置值，其次通过样本文本自动探测，最后回退默认1024维。Qdrant集合以仓库名称作为标识，若集合已存在则直接复用跳过重新索引，相似度采用余弦距离进行度量。

（4）Wiki文档生成

前端Wiki页面加载后建立WebSocket连接，向大语言模型发送生成请求。后端调用Qwen系列模型，基于向量检索的上下文生成结构化的Wiki文档。生成的Wiki以JSON格式缓存于服务器文件系统。

（5）增量索引刷新

增量索引通过git pull --ff-only获取远程更新，然后通过git diff检测文件变更。对于修改和删除的文件，移除对应Qdrant向量；对于新增和修改的文件，重新执行切分和嵌入。文件按批次处理，自动刷新调度器则按配置的时间间隔定期执行。用户也可通过API手动触发刷新。

Wiki生成模块的整体处理流程如图4-6所示。

图 4-6 Wiki生成模块流程图

### 智能问答模块详细设计

智能问答模块采用RAG技术架构，通过WebSocket协议实现实时流式交互。

（1）RAG检索

RAG检索的核心是首先加载或构建Qdrant向量索引，检查集合可用性，然后同步Embedder维度与Qdrant集合维度。此后进行执行语义检索：将用户查询文本通过嵌入模型转换为稠密向量，在Qdrant中执行余弦相似度检索，返回top-K个最相关的代码块及其元数据。

（2）上下文构建

上下文构建先将检索结果按文件路径分组格式化；然后从对话历史中提取多轮问答格式化为XML结构；最后将系统提示词、对话历史、文件内容、检索上下文和用户问题组装为最终Prompt。

（3）WebSocket流式生成

WebSocket连接建立后，接收请求数据并校验，提取用户问题，初始化RAG检索器，加载对话历史，执行语义检索获取相关代码上下文，组装Prompt后调用大模型进行生成。每个文本片段通过WebSocket实时推送到前端。

（4）容错降级策略

系统内置降级机制：当WebSocket连接失败时，前端自动切换到HTTP流式通道；当RAG检索异常时，返回空上下文由LLM基于自身知识回答；当嵌入维度不匹配时，自动以Qdrant集合维度为准重新进行用户问题的向量化。

智能问答模块的交互流程如图4-7所示。

图 4-7 智能问答模块流程图

### 深度研究模块详细设计

深度研究模块是在智能问答模块基础上的增强模块，通过服务端控制的多轮迭代实现逐层深入的代码分析。

（1）分阶段提示词设计

系统采用三套提示词模板对应不同迭代阶段：第1轮使用研究计划提示词，要求LLM制定研究方法和步骤，识别关键方面，提供初步发现；第2-4轮使用研究更新提示词，要求在前轮基础上深入聚焦特定方面，提供新见解，不重复已有内容；第5轮使用综合结论提示词，要求综合前四轮发现形成全面结论，直接回答原始问题。

（2）服务端迭代循环

在进入迭代循环前，系统一次性完成RAG检索器初始化、仓库元信息提取、文件内容获取和Qdrant语义检索，检索上下文在所有5轮中共享复用。迭代通过for循环显式控制：第1轮输出研究计划，第2-4轮输出研究更新（每轮携带前序累积的对话历史），第5轮输出最终结论。

（3）前端分页展示

前端解析标记符进行分页，每个标记对应一个研究阶段页面。当存在多个页面时，消息气泡顶部渲染分页栏，支持"上一页/下一页"切换。深度研究消息在数据库中messageType标记为deep_research，加载时自动启用分页解析，默认展示结论页。

深度研究模块的迭代流程如图4-8所示。

图 4-8 深度研究模块流程图

### 对话管理模块详细设计

对话管理模块负责用户与AI之间对话会话的全生命周期管理，包括对话和消息的增删改查以及反馈评价。

（1）数据模型

模块涉及三张核心表：conversations（对话历史表）通过user_id外键关联用户；conversation_messages（对话消息表）通过conversation_id外键关联对话，role字段限制为user 或assistant，message_type区分normal和deep_research；message_feedbacks（消息反馈表）通过message_id外键关联消息。三表之间通过级联删除策略保证数据一致性。

（2）消息反馈机制

对话管理模块支持赞（liked）和踩（disliked）评价。反馈前校验类型合法性、对话所有权和消息存在性。已反馈的消息不可重复提交（返回HTTP409）。点踩时可附带文字反馈说明问题。

（3）前端交互

对话管理模块左侧对话列表支持新建、选择和删除对话。对话标题自动取首条消息字符生成。消息采用乐观更新策略：用户消息立即渲染并保存，AI回答在流式完成后保存。每条消息有删除按钮，消息反馈支持点赞和点踩（点踩弹出文字输入弹窗）。

对话管理模块的操作流程如图4-9所示。

图 4-9 对话管理模块流程图

### 用户设置模块详细设计

用户设置模块允许用户个性化配置系统参数。

（1）配置数据模型

用户配置以JSON格式存储在user_settings表的config字段中，与users表一对一关联。系统定义三个配置子模块：向量化模块的dimension参数，默认为1024，控制嵌入向量维度；向量检索模块的top_k参数默认为20，控制RAG检索返回数量；自动更新模块的enabled参数，默认为true、interval_minutes参数，默认为60，控制增量索引自动刷新策略。

（2）配置生效流程

用户发起问答时后端从user_settings表查询嵌入维度值、Top-K值。若数据库中尚无用户配置记录，则返回默认值；若已有配置则使用用户设定的值。自动刷新调度器在配置变更时通过先停止再启动的方式实现热更新。

用户设置模块的配置流程如图4-10所示。

图 4-10 对话管理模块流程图

## 智能代码问答系统功能模块功能实现

下面部分介绍智能代码问答系统各模块功能的实现部分，将通过文字描述介绍功能接口实现、类图介绍功能代码实现和展示实现界面截图相结合的方式来讲解每个模块包含功能的具体实现，从代码层到界面展示层全面展示每个功能的实现情况。

### 用户认证模块功能实现

用户认证模块的实现包括的接口有 /auth/register、/auth/login、/auth/me。

/auth/register接口实现的是用户注册功能。/auth/register 接口对应的处理函数为auth_router.py中的register()函数，register()函数接收前端传入的用户名、邮箱和密码，通过SQLAlchemy ORM的User模型类与数据库交互，首先在users表中查询用户名和邮箱是否已被占用，已占用则返回HTTP 409错误；未被占用则调用_pwd_context对象的hash()方法将密码通过PBKDF2-SHA256算法哈希处理后存入password_hash字段，提交数据库后调用_create_access_token()函数签发JWT Token，返回Token和用户信息给前端。

/auth/login接口实现的是用户登录功能。/auth/login接口对应的处理函数为auth_router.py中的login()函数，login()函数通过User模型类在users表中以用户名为条件查询用户记录，查到后调用_pwd_context对象的verify()方法比对密码哈希值，同时检查is_active字段确认账户是否启用，验证通过后调用_create_access_token()函数签发JWT Token返回给前端。

/auth/me接口实现的是获取当前登录用户信息功能。/auth/me接口对应的处理函数为auth_router.py中的me()函数，该函数通过FastAPI的依赖注入机制先调用get_current_user()函数进行Token校验，get_current_user()函数从请求头提取Bearer Token，调用_decode_token()函数使用HS256算法验签并提取用户ID，再通过User模型类查询数据库确认用户存在且启用，最终将用户信息转换为UserOut模型返回。

用户认证模块功能实现对应的类图如图4-11所示。

图 4-11 用户认证模块功能类图

注册界面、登录界面实现如图4-12、图4-13所示。

图 4-12 注册界面

图 4-13 登录界面

### 仓库管理模块功能实现

仓库管理模块的实现集中在前端Dashboard页面组件中，包括仓库地址解析、配置弹窗管理和历史项目列表三个子功能。

仓库地址解析功能通过Dashboard页面组件中的parseRepositoryInput()函数实现，该函数使用三个正则表达式按顺序匹配输入内容：Windows路径正则匹配本地绝对路径，Unix绝对路径前缀 / 匹配本地路径，Git URL正则匹配远程仓库地址。解析成功后提取owner、repo、type等字段构造仓库信息对象，同时调用extractUrlDomain()和extractUrlPath()两个工具函数提取域名和路径信息用于类型判断。

配置弹窗管理功能通过ConfigurationModal.tsx组件实现。该组件包含语言选择、AI 模型选择、Wiki 类型设置、文件过滤器配置和访问令牌输入五个子配置项，用户调整完各项参数后点击提交，组件将配置序列化后通过localStorage.setItem()以仓库URL为键存入浏览器的键中实现配置缓存，随后调用router.push()跳转至/\[owner\]/\[repo\]页面进入Wiki生成流程。

历史项目列表功能通过ProcessedProjects.tsx组件实现。该组件在挂载时调用/api/wiki/projects接口获取用户已处理的项目列表数据，每个项目展示仓库名称、所有者和处理时间等信息，支持按项目名称、仓库名等关键词搜索过滤，用户点击项目即可快速进入对应Wiki页面。删除功能通过向/api/wiki/projects发送DELETE请求清除服务端缓存。

仓库管理模块功能实现对应的类图如图4-14所示。

图 4-14 仓库管理模块功能类图

仓库输入与配置弹窗界面、历史项目列表界面实现如图4-15、图4-16所示。

图 4-15 仓库输入与配置弹窗界面

图 4-16 历史项目列表界面

### Wiki生成模块功能实现

Wiki 生成模块的实现包括的接口有/local_repo/structure、POST/export/wiki以及DatabaseManager.prepare_database()。

/local_repo/structure接口实现的是获取本地仓库文件树和README内容的功能。前端Wiki页面加载时调用此接口，传入本地路径参数，后端读取文件系统返回文件树结构和README文件内容，前端基于这些数据通过大语言模型确定Wiki结构。

仓库下载功能通过data_pipeline.py中的download_repo()函数实现，该函数通过Python的subprocess模块执行git clone --depth=1 --single-branch命令进行浅克隆，若提供访问令牌则将Token编码后嵌入Git URL以访问私有仓库。文件读取通过read_all_documents()函数实现，该函数遍历仓库目录，应用排除/包含过滤器筛选文件，返回文档列表供后续拆分处理。

代码拆分功能通过code_splitter.py中的TreeSitterCodeSplitter类实现。TreeSitterCodeSplitter类内部维护EXTENSION_TO_LANGUAGE字典将文件扩展名映射到Tree-sitter语言标识，CHUNK_NODE_TYPES字典定义各语言的AST节点类型（如Python的function_definition、class_definition），调用Tree-sitter 解析器生成AST后遍历语法树并按语义单元切分代码，每个切分块附带文件路径、函数名、类名、起止行号等元数据。

向量嵌入与索引构建功能通过DatabaseManager类的prepare_db_index()方法实现。该方法首先检查Qdrant中是否已存在该仓库的集合，若存在则直接复用；若不存在，则通过一次样本嵌入探测确定向量维度，创建Qdrant集合后调用read_all_documents()获取文档列表，依次经过Tree-sitter拆分、Embedding向量化和Qdrant批量写入三步完成索引构建。

增量索引刷新功能通过incremental_index.py模块实现。incremental_update_for_repo()函数首先调用git_pull()函数执行git pull --ff-only获取远程更新，然后调用git diff --name-status获取变更文件列表并封装为RepoDiff对象，对于RepoDiff中的新增和修改文件重新执行拆分和嵌入流程，对于删除文件从Qdrant中移除对应向量点。定时调度通过_auto_refresh_loop()异步循环实现，按用户配置的间隔时间定期执行。

Wiki生成模块功能实现对应的类图如图4-17所示。

图 4-17 Wiki生成模块功能类图

Wiki树形结构展示界面、全面Wiki视图界面实现如图4-18、图4-19所示。

图 4-18 Wiki生成展示界面

图 4-19 全面Wiki视图界面

### 智能问答模块功能实现

智能问答模块的实现包括的WebSocket端点为/ws/chat，降级兜底HTTP端点为/chat/completions/stream，前端核心组件为Ask.tsx。

/ws/chat端点实现的是通过WebSocket协议的实时流式问答功能。/ws/chat端点对应的处理函数为websocket_wiki.py中的handle_websocket_chat()函数。WebSocke 连接建立后，前端Ask.tsx组件发送ChatCompletionRequest请求体（包含仓库 URL、对话消息列表、AI 模型配置、语言参数和文件过滤信息），handle_websocket_chat()函数接收请求后首先调用_prepare_rag()函数初始化 RAG检索器——该函数通过RAG 类的prepare_retriever()方法连接Qdrant向量数据库并从user_settings表查询用户配置的维度和Top-K参数；接着调用Memory类的add_dialog_turn()方法加载历史对话，调用_retrieve_context()函数执行语义检索获取相关代码上下文，调用_build_prompt()函数组装包含系统提示词、对话历史、文件内容和检索上下文的完整Prompt；最后通过DashscopeClient类的acall()方法向DashScope平台发送请求进行流式生成，生成的每个文本片段通过WebSocket实时推送到前端，前端Markdown组件逐Token渲染展示。

容错降级方面，当LLM返回Token超限错误时，\_stream_llm_response()函数自动调用_strip_context_from_prompt()函数将Prompt中的检索上下文区块移除后重新发起请求；当WebSocket连接失败时，前端自动降级至POST/chat/completions/stream HTTP端点，该端点对应的处理函数为simple_chat.py中的chat_completions_stream()函数，通过HTTP流式响应维持问答功能可用。

智能问答模块功能实现对应的类图如图4-20所示。

图 4-20 智能问答模块功能类图

智能问答界面、问答结果展示界面实现如图 4-21、图 4-22 所示。

图 4-21 智能问答界面

图 4-21 问答结果展示界面

### 深度研究模块功能实现

深度研究模块的实现包括的WebSocket端点为/ws/chat/deep-research，与普通问答共享前端Ask.tsx组件，通过深度研究开关区分模式。

/ws/chat/deep-research端点实现的是多轮迭代式的深度代码分析功能。/ws/chat/deep-research端点对应的处理函数为websocket_wiki.py中的handle_websocket_chat_deep_research()函数。WebSocket连接建立后，该函数接收前端提交的一个用户问题，首先一次性完成RAG检索器初始化、仓库信息提取和代码上下文检索（后续5轮迭代共享此检索结果以避免重复开销），然后进入最大5轮的服务端迭代循环。

第1轮迭代使用_DEEP_RESEARCH_PLAN_PROMPT提示词模板，要求模型制定研究计划、识别关键方面并提供初步发现。第2至4轮迭代使用_DEEP_RESEARCH_UPDATE_PROMPT提示词模板，每次迭代将之前所有轮次的问答内容累积为对话历史传给模型，要求模型在前序研究基础上识别空白点并针对性地深入分析，不重复已有内容。第5轮迭代使用_DEEP_RESEARCH_CONCLUSION_PROMPT提示词模板，要求模型综合前四轮的所有发现，提供包含具体代码引用和实现细节的完整结论。每轮迭代开头通过@@PAGE|type|title@@标记符分隔不同阶段的研究成果，前端Ask.tsx组件的parsePages()函数实时解析这些标记符将流式响应切分为多个ChatPage对象（类型包括plan、update、conclusion），用户通过页面顶部的导航条在各研究阶段之间自由切换浏览。

若任一迭代轮次的LLM调用失败（如Token超限），\_stream_llm_response()函数自动执行检索上下文降级重试；若降级仍然失败，则终止整体研究流程并关闭WebSocket连接。深度研究模块功能实现对应的类图如图4-23所示。

图 4-23 深度研究模块功能类图

深度研究界面、深度研究分页展示界面实现如图4-24、图4-25所示。

图 4-24 深度研究界面

图 4-25 深度研究分页展示界面

### 对话管理模块功能实现

对话管理模块的实现包括的接口有/api/conversations（GET/POST）、/api/conversations/{id}（DELETE）、/api/conversations/{id}/messages（GET/POST）、/api/conversations/{cid}/messages/{mid}（PATCH/DELETE）、/api/conversations/{cid}/messages/{mid}/feedback（POST），共计8个点，全部要求Bearer Token认证。

GET /api/conversations接口实现的是获取指定仓库下对话列表的功能。GET /api/conversations接口对应的处理函数为conversation_router.py中的list_conversations()函数，该函数接收仓库所有者和仓库名称两个查询参数，通过Conversation模型类在conversations表中查询当前用户在指定仓库下的所有对话记录，按更新时间倒序排列，通过_conversation_to_out()函数将ORM对象转换为ConversationOut响应模型返回。

POST /api/conversations接口实现的是创建新对话的功能。POST /api/conversations接口对应的处理函数为conversation_router.py中的create_conversation()函数，该函数接收仓库所有者、仓库名称和对话标题，通过UUID生成唯一对话ID，创建Conversation模型实例并写入数据库，返回新创建的对话信息。

DELETE/api/conversations/{id}接口实现的是删除对话及其关联消息的功能。该接口对应的处理函数为conversation_router.py中的delete_conversation()函数，首先验证对话是否属于当前用户，验证通过后执行删除操作，由于Conversation模型与ConversationMessage模型配置了cascade="all,delete-orphan"级联删除策略，删除对话时数据库自动清理其下所有消息记录。

GET/api/conversations/{id}/messages和POST/api/conversations/{id}/messages接口分别实现获取消息列表和创建消息的功能，对应的处理函数为get_conversation_messages()和create_conversation_message()函数。获取消息时按创建时间正序排列并转换为MessageOut响应模型；创建消息时验证消息角色（user或assistant）和对话归属，写入数据库后同步更新对话的时间戳。

POST /api/conversations/{cid}/messages/{mid}/feedback接口实现的是消息反馈提交功能。该接口对应的处理函数为conversation_router.py中的submit_message_feedback()函数，接收反馈类型（liked或disliked）和可选的文字反馈内容。该函数首先检查消息是否已被反馈过，若feedback_status字段已有值则返回HTTP 409错误禁止重复提交；若未反馈则创建新的MessageFeedback记录，同时同步更新ConversationMessage表的feedback_status字段以快速展示反馈状态。

PATCH /api/conversations/{cid}/messages/{mid}接口实现的是更新消息内容的功能，允许修改消息正文和消息类型字段。

对话管理模块功能实现对应的类图如图4-26所示。

图 4-26 对话管理模块功能类图

对话列表与新建对话界面、对话消息与反馈界面实现如图4-27、图4-28所示。

图 4-27 对话列表与新建对话界面

图 4-28-1 对话消息与反馈界面(点赞)

图 4-28-1 对话消息与反馈界面(点踩与反馈)

### 用户设置模块功能实现

用户设置模块的实现包括的接口有/api/user/settings（GET/PUT）。

GET /api/user/settings接口实现的是读取当前用户个性化配置的功能。GET /api/user/settings接口对应的处理函数为settings_router.py中的get_user_settings()函数，该函数通过get_current_user获取当前登录用户，通过UserSettings模型类在user_settings表中查询该用户的配置记录。若记录不存在则返回_DEFAULT_CONFIG常量定义的默认配置（嵌入维度1024、Top-K为20、自动刷新启用且间隔60分钟）；若记录存在则通过_row_config()函数提取三个子模块的配置数据返回。

PUT /api/user/settings接口实现的是更新用户个性化配置的功能。PUT /api/user/settings接口对应的处理函数为settings_router.py中的update_user_settings()函数，该函数首先查询用户配置记录是否存在,不存在则创建一条新记录，存在则更新：通过_ensure_dict()函数遍历三个子模块键名（embedding、retrieval、auto_refresh），仅将请求体中传入的子模块字段合并到现有配置中，保留未传入字段的原有值。

用户设置模块功能实现对应的类图如图4-29所示。

图 4-29 用户设置模块功能类图

用户设置界面实现如图 4-30 所示。

图 4-29 用户设置界面

## 本章小结

本章完成了对智能代码问答系统的详细设计，按功能模块进行设计，每个模块分别通过活动图或流程图描述该模块设计的具体实现过程。然后根据完成的详细设计内容分别实现系统后台管理端和用户端每个模块包含的功能，给出每个功能对应的实现接口，展示模块实现对应类图和实现界面。截至目前，系统的开发工作基本完成

# 智能代码问答系统的测试

## 引言

本章开始对智能代码问答系统进行测试工作，在这一阶段将对系统各个模块进行全面的功能测试。这包括设计针对不同功能模块的测试用例和执行，以确保系统在各种情况下都能够正常运行并满足用户需求。通过测试，我们将验证系统的稳定性、可靠性和性能，并及时发现并解决潜在的问题，以保证系统的质量和可用性。

## 功能模块测试

### 用户认证模块功能测试

用户登录功能测试结果如表5-1所示。

表 5-1 用户认证模块功能测试结果

| **所属等价类** | **测试数据** | **预期结果** | **实际结果** |
| --- | --- | --- | --- |
| 用户名为空 | 不输入用户名，密码输入正确 | 用户名输入框标红，提示"用户名不能为空" | 与预期结果相同 |
| 密码为空 | 用户名输入正确，不输入密码 | 密码输入框标红，提示"密码不能为空" | 与预期结果相同 |
| 用户名不存在 | 用户名：nonexistent，密码：123456 | 提示"用户名或密码错误" | 与预期结果相同 |
| 密码错误 | 用户名：testuser，密码：wrongpass | 提示"用户名或密码错误" | 与预期结果相同 |
| 用户名和密码都正确 | 用户名：testuser，密码：123456 | 登录成功并跳转至主页面 | 与预期结果相同 |

用户注册功能测试结果如表5-2所示。

表 5-2 用户注册功能测试结果

| **所属等价类** | **测试数据** | **预期结果** | **实际结果** |
| --- | --- | --- | --- |
| 用户名为空 | 不输入用户名，邮箱、密码等其他信息正确 | 用户名输入框标红，提示"用户名不能为空" | 与预期结果相同 |
| 邮箱格式不正确 | 用户名：testuser2，邮箱：invalid-email，密码：123456 | 邮箱输入框标红，提示"请输入有效的邮箱地址" | 与预期结果相同 |
| 密码长度不足6位 | 用户名：testuser2，邮箱：test2@example.com，密码：12345 | 密码输入框标红，提示"密码长度至少为6位" | 与预期结果相同 |
| 用户名已存在 | 用户名：testuser（已注册），邮箱：test2@example.com，密码：123456 | 注册失败，提示"用户名已存在" | 与预期结果相同 |
| 邮箱已存在 | 用户名：testuser2，邮箱：test@example.com（已注册），密码：123456 | 注册失败，提示"邮箱已被注册" | 与预期结果相同 |

JWT令牌验证测试结果如表5-3所示。

表 5-3 JWT令牌验证测试结果

| **所属等价类** | **测试数据** | **预期结果** | **实际结果** |
| --- | --- | --- | --- |
| 有效令牌 | 使用登录成功返回的有效JWT令牌 | 验证通过，允许访问受保护接口 | 与预期结果相同 |
| 无令牌 | 请求头中不包含Authorization字段 | 返回401未授权错误 | 与预期结果相同 |
| 令牌格式错误 | Authorization头格式不正确（如缺少Bearer前缀） | 返回401未授权错误 | 与预期结果相同 |
| 令牌过期 | 使用已过期的JWT令牌 | 返回401未授权错误 | 与预期结果相同 |
| 令牌被篡改 | 使用被篡改的JWT令牌 | 返回401未授权错误 | 与预期结果相同 |

### 仓库管理模块功能测试

仓库地址解析测试结果如表5-4所示。

表 5-4 仓库地址解析测试结果

| **所属等价类** | **测试数据** | **预期结果** | **实际结果** |
| --- | --- | --- | --- |
| HTTPS Git URL | https://github.com/octocat/Hello-World.git | 成功解析，识别为GitHub仓库，仓库名：Hello-World | 与预期结果相同 |
| SSH Git URL | git@github.com:octocat/Hello-World.git | 成功解析，识别为GitHub仓库，仓库名：Hello-World | 与预期结果相同 |

表5-4（续表）

| **所属等价类** | **测试数据** | **预期结果** | **实际结果** |
| --- | --- | --- | --- |
| Windows本地路径 | D:\\bishe\\codewiki | 成功解析，识别为本地仓库，仓库名：codewiki | 与预期结果相同 |
| UNIX本地路径 | /home/user/project | 成功解析，识别为本地仓库，仓库名：project | 与预期结果相同 |
| 空仓库地址 | 不输入任何仓库地址 | 输入框标红，提示"请输入仓库地址" | 与预期结果相同 |

仓库配置测试结果如表5-5所示。

表 5-5 仓库配置测试结果

| **所属等价类** | **测试数据** | **预期结果** | **实际结果** |
| --- | --- | --- | --- |
| 打开配置弹窗 | 点击仓库配置按钮 | 配置弹窗正常显示，包含文件过滤、AI模型等选项 | 与预期结果相同 |
| 修改文件过滤列表 | 添加/移除文件扩展名（如.md、.txt） | 配置实时更新，保存到localStorage | 与预期结果相同 |
| 切换AI模型 | 选择不同的LLM模型（如qwen-turbo、qwen-plus） | 模型选择更新，保存到localStorage | 与预期结果相同 |
| 关闭配置弹窗 | 点击关闭按钮或弹窗外部 | 配置弹窗正常关闭 | 与预期结果相同 |
| 刷新页面恢复配置 | 配置后刷新页面 | 配置从localStorage恢复，与之前设置一致 | 与预期结果相同 |

历史项目管理测试结果如表5-6所示。

表 5-6 历史项目管理测试结果

| **所属等价类** | **测试数据** | **预期结果** | **实际结果** |
| --- | --- | --- | --- |
| 显示历史项目列表 | 首次访问或有历史项目 | 显示历史项目列表，按访问时间排序 | 与预期结果相同 |
| 点击历史项目 | 点击历史项目列表中的项目 | 自动填充仓库地址并加载该项目 | 与预期结果相同 |
| 删除历史项目 | 点击历史项目的删除按钮 | 该项目从历史列表中移除 | 与预期结果相同 |
| 无历史项目 | 首次使用或已清空所有历史 | 显示"暂无历史项目"提示 | 与预期结果相同 |

### Wiki生成模块功能测试

代码文件识别测试结果如表5-7所示。

表 5-7 代码文件识别测试结果

| **所属等价类** | **测试数据** | **预期结果** | **实际结果** |
| --- | --- | --- | --- |
| Python文件 | test.py | 识别为Python语言，使用对应解析器 | 与预期结果相同 |
| JavaScript文件 | test.js、test.jsx | 识别为JavaScript语言，使用对应解析器 | 与预期结果相同 |
| TypeScript文件 | test.ts、test.tsx | 识别为TypeScript语言，使用对应解析器 | 与预期结果相同 |
| Java文件 | test.java | 识别为Java语言，使用对应解析器 | 与预期结果相同 |
| Go文件 | test.go | 识别为Go语言，使用对应解析器 | 与预期结果相同 |
| Rust文件 | test.rs | 识别为Rust语言，使用对应解析器 | 与预期结果相同 |
| C/C++文件 | test.c、test.cpp、test.h | 识别为C/C++语言，使用对应解析器 | 与预期结果相同 |
| 非代码文件 | test.md、test.txt、test.json | 正确过滤或降级处理 | 与预期结果相同 |
| 未知扩展名文件 | test.xyz | 降级使用固定长度拆分器 | 与预期结果相同 |

代码拆分功能测试结果如表5-8所示。

表 5-8 代码拆分功能测试结果

| **所属等价类** | **测试数据** | **预期结果** | **实际结果** |
| --- | --- | --- | --- |
| Python函数提取 | 包含独立函数的Python代码 | 成功提取函数，包含完整函数体 | 与预期结果相同 |
| Python类提取 | 包含类定义的Python代码 | 成功提取类，包含所有方法 | 与预期结果相同 |
| JavaScript函数提取 | 包含function定义的JS代码 | 成功提取函数 | 与预期结果相同 |
| JavaScript类提取 | 包含class定义的JS代码 | 成功提取类 | 与预期结果相同 |
| Java类提取 | 包含类和方法的Java代码 | 成功提取类和方法 | 与预期结果相同 |
| 长代码拆分 | 超过1500字符的长代码 | 拆分为多个Chunk，有重叠部分 | 与预期结果相同 |
| 短代码处理 | 少于1500字符的短代码 | 作为单个Chunk处理 | 与预期结果相同 |
| Chunk元数据完整性 | 任意代码块 | 每个Chunk包含file_path、language、start_line、end_line | 与预期结果相同 |

向量索引功能测试结果如表5-9所示。

表 5-9 向量索引功能测试结果

| **所属等价类** | **测试数据** | **预期结果** | **实际结果** |
| --- | --- | --- | --- |
| 集合名生成 | 仓库名：myrepo | 生成集合名：codewiki_myrepo | 与预期结果相同 |
| 集合名特殊字符处理 | 仓库名：owner/repo-name.git | 生成集合名：codewiki_ownerrepo-name | 与预期结果相同 |
| 单Chunk插入 | 1个代码块和对应向量 | 插入成功，count()返回1 | 与预期结果相同 |
| 多Chunk插入 | 10个代码块 | 插入成功，count()返回10 | 与预期结果相同 |
| Payload完整存储 | 包含完整元数据的Chunk | 搜索结果中保留所有元数据 | 与预期结果相同 |
| 语义搜索 | 查询向量，top_k=3 | 返回3个最相关的代码块，按相似度排序 | 与预期结果相同 |
| 空集合搜索 | 空集合，任意查询 | 返回空列表 | 与预期结果相同 |
| 集合重建 | 插入数据后重建集合 | count()回到0，数据清空 | 与预期结果相同 |

### 智能问答模块功能测试

流式问答功能测试结果如表5-10所示。

表 5-10 流式问答功能测试结果

| **所属等价类** | **测试数据** | **预期结果** | **实际结果** |
| --- | --- | --- | --- |
| WebSocket连接建立 | 有效的仓库地址和查询问题 | WebSocket连接成功建立 | 与预期结果相同 |
| 流式输出开始 | 发送查询请求 | 服务端开始逐块返回生成内容 | 与预期结果相同 |
| 流式输出完成 | 完整查询请求 | 服务端返回完整的回答内容 | 与预期结果相同 |
| 代码块引用 | 查询涉及代码的问题 | 回答中引用相关代码块，包含文件路径和行号 | 与预期结果相同 |
| RAG检索生效 | 查询与代码库相关的问题 | 检索并使用相关代码块作为上下文 | 与预期结果相同 |
| 对话历史管理 | 多轮对话 | 正确使用历史对话上下文 | 与预期结果相同 |
| WebSocket连接失败 | 模拟WebSocket连接失败 | 自动降级到HTTP流式端点 | 与预期结果相同 |

HTTP降级测试结果如表5-11所示。

表 5-11 HTTP降级测试结果

| **所属等价类** | **测试数据** | **预期结果** | **实际结果** |
| --- | --- | --- | --- |
| 基本请求格式 | repo_url、messages、可选file_path | 请求格式正确，服务端接受 | 与预期结果相同 |
| 流式响应格式 | 发送查询请求 | 响应以SSE格式流式返回 | 与预期结果相同 |
| 无仓库索引 | 查询未索引的仓库 | 提示需要先创建Wiki索引 | 与预期结果相同 |
| 空查询 | 发送空的查询内容 | 提示"请输入查询内容" | 与预期结果相同 |
| 超大Token输入 | 输入超大Token数量 | 触发Token超限处理策略 | 与预期结果相同 |

### 深度研究模块功能测试

深度研究流程测试结果如表5-12所示。

表 5-12 深度研究流程测试结果

| **所属等价类** | **测试数据** | **预期结果** | **实际结果** |
| --- | --- | --- | --- |
| 深度研究模式激活 | 开启深度研究模式开关 | 进入深度研究模式 | 与预期结果相同 |
| 5轮迭代执行 | 发送深度研究请求 | 执行5轮迭代：1轮计划+3轮更新+1轮总结 | 与预期结果相同 |
| 计划阶段输出 | 第1轮迭代 | 输出研究计划，以@@PAGE\|plan\|研究计划@@标记开头 | 与预期结果相同 |
| 更新阶段输出 | 第2-4轮迭代 | 输出深度更新内容，以@@PAGE\|update\|更新标题@@标记开头 | 与预期结果相同 |
| 结论阶段输出 | 第5轮迭代 | 输出综合结论，以@@PAGE\|conclusion\|结论@@标记开头 | 与预期结果相同 |
| 每轮上下文检索 | \-  | 执行代码库检索，获取相关上下文 | 与预期结果相同 |

前端分页显示测试结果如表5-13所示。

表 5-13 前端分页显示测试结果

| **所属等价类** | **测试数据** | **预期结果** | **实际结果** |
| --- | --- | --- | --- |
| 分页标记解析 | 收到含@@PAGE@@标记的输出 | 正确解析分页标记 | 与预期结果相同 |
| 导航栏生成 | 多个分页内容 | 生成页面导航栏，显示所有页面标题 | 与预期结果相同 |
| 页面切换 | 点击导航栏不同页面 | 切换显示对应页面内容 | 与预期结果相同 |
| 切换深度研究 | 点击切换按钮 | 在深度研究与普通问答模式直接切换 | 与预期结果相同 |

### 对话管理模块功能测试

对话CRUD功能测试结果如表5-14所示。

表 5-14 对话CRUD功能测试结果

| **所属等价类** | **测试数据** | **预期结果** | **实际结果** |
| --- | --- | --- | --- |
| 创建新对话 | 仓库信息和对话标题 | 对话创建成功，返回对话ID | 与预期结果相同 |
| 获取对话列表 | 当前仓库参数 | 返回用户在该仓库下的所有对话 | 与预期结果相同 |
| 获取对话详情 | 有效对话ID | 返回对话详情和所有消息 | 与预期结果相同 |
| 更新对话标题 | 对话ID和新标题 | 对话标题更新成功 | 与预期结果相同 |
| 访问他人对话 | 其他用户的对话ID | 返回403禁止访问错误 | 与预期结果相同 |
| 无效对话ID | 不存在的对话ID | 返回404未找到错误 | 与预期结果相同 |

消息管理功能测试结果如表5-15所示。

表 5-15 消息管理功能测试结果

| **所属等价类** | **测试数据** | **预期结果** | **实际结果** |
| --- | --- | --- | --- |
| 发送新消息 | 对话ID和消息内容 | 消息创建成功，返回消息详情 | 与预期结果相同 |
| 获取消息列表 | 对话ID | 返回该对话的所有消息，按时间排序 | 与预期结果相同 |
| 编辑消息 | 消息ID和新内容 | 消息内容更新成功 | 与预期结果相同 |
| 删除消息 | 消息ID | 消息删除成功 | 与预期结果相同 |
| 消息点赞 | 消息ID和feedback_type=like | 反馈保存成功 | 与预期结果相同 |
| 消息点踩 | 消息ID和feedback_type=dislike | 反馈保存成功 | 与预期结果相同 |

### 用户设置模块功能测试

设置获取功能测试结果如表5-16所示。

表 5-16 设置获取功能测试结果

| **所属等价类** | **测试数据** | **预期结果** | **实际结果** |
| --- | --- | --- | --- |
| 首次获取设置 | 无设置记录的用户 | 返回默认配置：embedding、retrieval、auto_refresh | 与预期结果相同 |
| 获取已有设置 | 有设置记录的用户 | 返回数据库中的完整配置 | 与预期结果相同 |
| 未授权访问 | 使用其他用户的Token | 返回403禁止访问错误 | 与预期结果相同 |

设置更新功能测试结果如表5-17所示。

表 5-17 设置更新功能测试结果

| **所属等价类** | **测试数据** | **预期结果** | **实际结果** |
| --- | --- | --- | --- |
| 增量更新单字段 | 只更新retrieval.top_k=5 | 其他字段保持不变，仅top_k更新为5 | 与预期结果相同 |
| 增量更新多字段 | 更新embedding.dimension=1024和auto_refresh.interval_minutes=30 | 两个字段均更新，其他不变 | 与预期结果相同 |
| 完整覆盖更新 | 提交完整三个子模块配置 | 所有字段按输入更新 | 与预期结果相同 |
| 空更新请求 | 提交空对象{} | 配置保持不变 | 与预期结果相同 |
| 无效配置值 | retrieval.top_k=-1 | 返回400错误，提示配置值无效 | 与预期结果相同 |

配置生效测试结果如表5-18所示。

表 5-18 配置生效测试结果

| **所属等价类** | **测试数据** | **预期结果** | **实际结果** |
| --- | --- | --- | --- |
| retrieval.top_k生效 | 设置top_k=3 | RAG检索返回3个结果 | 与预期结果相同 |
| embedding.model生效 | 设置不同的embedding模型 | 使用指定的模型生成向量 | 与预期结果相同 |
| auto_refresh.enabled生效 | 设置auto_refresh.enabled=true | 启用增量索引自动刷新 | 与预期结果相同 |
| auto_refresh.enabled=false | 设置auto_refresh.enabled=false | 禁用增量索引自动刷新 | 与预期结果相同 |

## 本章小结

本章内容主要是对系统包括的功能模块进行测试用例设计和执行，针对系统的每一个功能可能遇到的各种情况进行黑盒测试用例设计，并且完成测试用例的测试，根据记录的测试结果及时对系统中存在的问题进行修复，修复后再次测试相关测试用例直到功能达到预期效果，这个迭代过程一直持续到整个系统运行没有重大问题出现。迭代测试确保了系统功能的正确性，提高了系统的可靠性。

# 结 论

本文从解决大型软件项目中文档滞后、知识碎片化和跨文件理解成本高等实际痛点出发，设计并实现了一套基于检索增强生成（RAG）技术的智能代码问答系统。系统以"先定位证据、再生成答案"为核心范式，实现了代码仓库知识的自动提取、持续更新与高质量问答，从而提升开发者的代码理解效率、降低知识传承成本、增强跨团队协作能力。开发过程中首先对开发者日常编码场景进行需求分析，明确系统功能性需求与非功能性需求；在需求分析基础上进行概要设计，完成系统四层架构设计、七个功能模块划分、数据库实体关系与概念模型设计以及前后端接口定义设计；随后对系统每个功能模块进行详细设计，通过流程图和活动图描述具体实现流程；在设计完成后，进行系统的编码开发工作，搭建前后端项目并实现全部功能模块，对系统实现情况进行界面展示；最后针对每个功能模块设计测试用例并完成系统测试与调试工作。

本文主要完成的工作如下：
1. 设计并实现了基于RAG的智能代码问答系统。完成了用户认证模块、仓库管理模块、Wiki生成模块、智能问答模块、深度研究模块、对话管理模块和用户设置模块七个核心功能模块的需求分析、概要设计、详细设计与编码实现。前端采用Next.js+React+TypeScript+Tailwind CSS技术栈，后端采用Python FastAPI+SQLAlchemy ORM框架，前后端通过RESTful API与WebSocket进行数据交互，向量数据库使用Qdrant，关系型数据库使用MySQL，AI服务层采用DashScope平台的通义千问系列模型。
2. 设计并实现了基于Tree-sitter的语义级代码拆分与知识库构建功能。通过Tree-sitter增量式语法解析器对源代码进行函数级、类级等语义单元的精确切分，保留函数签名、参数列表、起止行号和文档注释等完整元数据；将切分后的代码片段通过Embedding嵌入模型向量化后存入Qdrant向量数据库，建立支持多仓库、多语言的高效语义检索知识库。
3. 设计并实现了增量索引与版本同步机制。通过定时任务周期性执行git pull检查仓库更新，利用git diff识别文件变更，仅对新增、修改和删除的文件执行增量解析与向量更新操作，未变更文件保持不变，确保知识库与代码版本实时同步，避免因文档滞后造成的信息失真。
4. 设计并实现了基于WebSocket的流式RAG智能问答功能。将用户自然语言查询向量化后在Qdrant中执行相似度检索，召回高相关代码片段作为证据集，结合结构化System Prompt调用大语言模型生成可追溯、可验证的结构化答案，并内置Token超限降级和WebSocket失败自动切换HTTP流式的容错策略。
5. 设计并实现了多轮迭代深度研究功能。通过服务端控制的5轮迭代循环，引导大语言模型逐层深入分析代码问题，分阶段输出研究计划、研究更新和最终结论，前端以分页形式展示多阶段研究成果，满足开发者对复杂代码逻辑的深入理解需求。

目前系统虽然实现了预期功能，但仍有值得改进和优化的地方，如：
1. 系统当前仅支持通义千问系列模型，后续可扩展接入更多大语言模型提供商（如DeepSeek、文心一言等），通过统一的模型适配层实现多模型灵活切换，提升系统的通用性和可选性。
2. Wiki生成的文档结构较为固定，可进一步引入自定义模板机制，允许用户根据项目类型和个人偏好定义Wiki文档的章节结构和内容粒度，使生成的文档更贴合实际使用需求。
3. 深度研究模块目前固定为5轮迭代，可引入自适应终止策略，由模型自主判断研究是否已充分，避免不必要的计算开销。
4. 系统在前端交互体验上还可进一步优化，例如增加代码片段高亮对比、检索结果可视化排序等功能，进一步提升开发者的使用体验。

# 参考文献

［1］林来兴. 空间控制技术［M］. 北京：中国宇航出版社，1992：25-42.

［2］辛希孟. 信息技术与信息服务国际研讨会论文集：A集［C］. 北京：中

国科学出版社，1999：45-49.

［3］赵耀东. 新时代的工业工程师［M/OL］. 台北：天下文化出版社，1998 ［1998-09-26］. http://www.ie.nthu.edu.tw/info/ie.newie.htm（Big5）.

……

［12］谌颖. 空间交会控制理论与方法研究［D］. 哈尔滨：哈尔滨工业大学，1992：8-13.

［13］ KANAMORI H. Shaking without quaking［J］. Science, 1998，279（5359）：2063-2064.

……

［104］CHRISTINE M. Plant Physiology: Plant Biology in the Genome Era［J/OL］. Science，1998，281：331-332［1998-09-23］. http://www. sciencemag. org/cgi/ collection/anatmorp.

……

# 攻读学士学位期间取得创新性成果

**一、发表的学术论文**

［1］ ×××，×××. Static Oxidation Model of Al-Mg/C Dissipation Thermal Protection Materials［J］. Rare Metal Materials and Engineering, 2010, 39(Suppl. 1): 520-524.（SCI收录，IDS号为669JS）

［2］ ×××，×××. 精密超声振动切削单晶铜的计算机仿真研究［J］. 系统仿真学报，2007，19（4）：738-741，753.（EI收录号：20071310514841）

［3］ ×××，×××. 局部多孔质气体静压轴向轴承静态特性的数值求解［J］. 摩擦学学报，2007（1）：68-72.（EI收录号：20071510544816）

［4］ ×××，×××. 硬脆光学晶体材料超精密切削理论研究综述［J］. 机械工程学报，2003，39（8）：15-22.（EI收录号：2004088028875）

［5］ ×××，×××. 基于遗传算法的超精密切削加工表面粗糙度预测模型的参数辨识以及切削参数优化［J］. 机械工程学报，2005，41（11）：158-162.（EI收录号：2006039650087）

［6］ ×××，×××. Discrete Sliding Mode Cintrok with Fuzzy Adaptive Reaching Law on 6-PEES Parallel Robot［C］. Intelligent System Design and Applications, Jinan, 2006: 649-652.（EI收录号：20073210746529）

**二、申请及已获得的专利（无专利时此项不必列出）**

［1］ ×××，×××. 一种温热外敷药制备方案：中国，88105607.3［P］. 1989-07-26.

**三、参与的科研项目及获奖情况**

［1］ ×××，×××. ××气体静压轴承技术研究, ××省自然科学基金项目.课题编号：××××.

［2］ ×××，×××. ××静载下预应力混凝土房屋结构设计统一理论. 黑江省科学技术二等奖, 2007.

# 哈尔滨工业大学本科毕业论文（设计）原创性声明和使用权限

本科毕业论文（设计）原创性声明

本人郑重声明：此处所提交的本科毕业论文（设计）《 》，是本人在导师指导下，在哈尔滨工业大学攻读学士学位期间独立进行研究工作所取得的成果，且毕业论文（设计）中除已标注引用文献的部分外不包含他人完成或已发表的研究成果。对本毕业论文（设计）的研究工作做出重要贡献的个人和集体，均已在文中以明确方式注明。

作者签名： 日期： 年 月 日

本科毕业论文（设计）使用权限

本科毕业论文（设计）是本科生在哈尔滨工业大学攻读学士学位期间完成的成果，知识产权归属哈尔滨工业大学。本科毕业论文（设计）的使用权限如下：

（1）学校可以采用影印、缩印或其他复制手段保存本科生上交的毕业论文（设计），并向有关部门报送本科毕业论文（设计）；（2）根据需要，学校可以将本科毕业论文（设计）部分或全部内容编入有关数据库进行检索和提供相应阅览服务；（3）本科生毕业后发表与此毕业论文（设计）研究成果相关的学术论文和其他成果时，应征得导师同意，且第一署名单位为哈尔滨工业大学。

保密论文在保密期内遵守有关保密规定，解密后适用于此使用权限规定。

本人知悉本科毕业论文（设计）的使用权限，并将遵守有关规定。

作者签名： 日期： 年 月 日

导师签名： 日期： 年 月 日

# 致 谢

衷心感谢导师×××教授对本人的精心指导。他的言传身教将使我终身受益。

感谢×××教授，以及实验室全体老师和同窗们的热情帮助和支持！

本课题承蒙××××基金资助，特此致谢。

……